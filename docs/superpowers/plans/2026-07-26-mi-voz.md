# mi-voz — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Una app web donde la persona graba su voz leyendo afirmaciones y escucha piezas de ~3 minutos con esa voz limpia sobre un fondo de lluvia, río o mar, mezcladas en vivo dentro de la app.

**Architecture:** Todo el audio se procesa en el navegador. No se genera ningún archivo final: se guarda la voz limpia ("voz master") más marcas de tiempo de las frases, y el navegador mezcla voz + fondo en cada reproducción. El servidor solo sirve la app, guarda la voz en Supabase Storage privado y orquesta el pulido con IA (única llamada externa).

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript 5 · Tailwind 4 · Supabase (Auth + Postgres schema `voz` + Storage privado) · Web Audio API · pruebas con el runner nativo de Node.

## Global Constraints

Estas reglas aplican a **todas** las tareas.

- **Todo se nombra en español**: carpetas, archivos, funciones, variables, columnas de base de datos, textos de interfaz y mensajes de commit. Es el patrón de `lamata-premios` y no se rompe.
- **Versiones exactas** (copiadas de `lamata-premios`): `next@16.2.11`, `react@19.2.4`, `react-dom@19.2.4`, `@supabase/ssr@^0.12.3`, `@supabase/supabase-js@^2.110.8`, `zod@^4.4.3`, `tailwindcss@^4`, `typescript@^5`, `server-only@^0.0.1`.
- **Node 22 o superior** (el runner de pruebas usa globs y `--experimental-strip-types`).
- **Pruebas sin librerías**: `node:test` + `node:assert/strict`. Script: `"test": "node --test --experimental-strip-types \"src/**/*.test.ts\""`. No se instala vitest, jest ni nada parecido.
- **Los imports en archivos de prueba llevan extensión `.ts`** (lo exige `--experimental-strip-types`).
- **Base de datos**: schema `voz` dentro del proyecto Supabase existente `mandarina-DATA` (`piingkecjgoisnxccvaa`), conviviendo con `crm`, `inbox`, `mata` y `rrhh`. **No se crea proyecto Supabase nuevo** (costaría ~$10/mes de cómputo extra).
- **Un schema nuevo no lo expone Supabase solo**: hay que agregar `voz` a `pgrst.db_schemas` o la API responde `406 Invalid schema`. Esto ya mordió en el proyecto `rrhh`.
- **Variables de entorno**: leerlas siempre con el helper `leerEntorno()` de la Tarea 1. Cargarlas a Vercel desde PowerShell les pega un BOM invisible que revienta en producción con `TypeError ... ByteString ... 65279`.
- **Nunca se ofrece descargar, exportar ni compartir un archivo de audio.** No hay botón de descarga, ni `Content-Disposition: attachment`, ni URL pública de la voz. Si un paso parece pedirlo, está mal leído.
- **RLS activo en todas las tablas.** Cada persona solo ve lo suyo.
- **Commits frecuentes**, uno por tarea como mínimo, en español y sin `git add -A` (agregar rutas explícitas).

---

## Estructura de archivos

```
src/
  app/
    layout.tsx                        Marco visual y fuentes
    page.tsx                          Portada pública
    (auth)/ingresar/page.tsx          Pedir celular
    (auth)/ingresar/acciones.ts       Server action: mandar código
    (auth)/verificar/page.tsx         Pedir código de 6 dígitos
    (auth)/verificar/Formulario.tsx   Cliente: input del código
    (auth)/verificar/acciones.ts      Server action: validar código
    (app)/estudio/page.tsx            Elegir paquete y editar el texto
    (app)/estudio/acciones.ts         Guardar texto, crear grabación
    (app)/grabar/[textoId]/page.tsx   Teleprompter + grabador
    (app)/grabar/[textoId]/Grabador.tsx
    (app)/mezclar/[grabacionId]/page.tsx
    (app)/mezclar/[grabacionId]/Mezclador.tsx
    (app)/cuenta/page.tsx             Plan, consentimiento, borrar todo
    (app)/cuenta/acciones.ts
    api/pulir/route.ts                Dispara el pulido con IA
    api/pulir/estado/route.ts         Consulta el estado del pulido
  lib/
    entorno.ts                        Lee variables de entorno sin BOM
    entorno.test.ts
    afirmaciones/paquetes.ts          Los 5 paquetes, en código
    afirmaciones/paquetes.test.ts
    audio/plan.ts                     Planificador de mezcla (núcleo puro)
    audio/plan.test.ts
    audio/frases.ts                   Detección de frases por silencios
    audio/frases.test.ts
    audio/reproductor.ts              Ejecuta el plan con Web Audio
    audio/grabacion.ts                Formato soportado por el navegador
    audio/grabacion.test.ts
    pulido/proveedor.ts               Interfaz del servicio de pulido
    pulido/elegido.ts                 Implementación seleccionada
    sinInternet.ts                    Guardado local de voz y fondos
    supabase/esquema.ts               export const SCHEMA = "voz"
    supabase/servidor.ts
    supabase/cliente.ts
    supabase/admin.ts
    telefono.ts                       Normalizar celular a E.164
    telefono.test.ts
    planes.ts                         Límites de gratis vs pago
    planes.test.ts
  proxy.ts                            Refresca sesión y protege /estudio
                                      (en Next 16 esto ya NO es middleware.ts)
supabase/
  migrations/0001_esquema_voz.sql
  migrations/0002_rls_voz.sql
  migrations/0003_almacen_voces.sql
public/
  fondos/lluvia.mp3  rio.mp3  mar.mp3  bosque.mp3  tormenta.mp3
```

---

## Tarea 0: Elegir el servicio de pulido (la hace Rodrigo, no un agente)

**Bloquea únicamente la Tarea 7.** Las tareas 1 a 6 avanzan sin esto.

Esta es la prueba que decide si el producto es viable. Si la voz de una persona común, grabada con el micrófono de un celular en un cuarto normal, no queda sonando bien, no hay app que salvar.

- [ ] **Paso 1: Grabar el material de prueba**

Con el celular, en un cuarto normal (con ruido de calle o ventilador, **no** en silencio de estudio), grabar 2 minutos leyendo cinco afirmaciones, dejando a propósito un par de muletillas y una pausa larga. Guardar como `pruebas/voz-cruda.m4a`.

- [ ] **Paso 2: Pasarla por los tres candidatos**

Subir ese mismo archivo a los tres y guardar el resultado:

| Servicio | Dónde | Gratis al mes | Guardar como |
|---|---|---|---|
| Auphonic | auphonic.com | 2 horas | `pruebas/auphonic.mp3` |
| Dolby.io Media Enhance | dolby.io → Media Enhance | 200 minutos | `pruebas/dolby.mp3` |
| ElevenLabs Voice Isolator | elevenlabs.io → Voice Isolator | 10.000 créditos ≈ 10 min | `pruebas/elevenlabs.mp3` |

**Esta prueba cuesta $0**: los tres planes gratuitos cubren de sobra 2 minutos de audio.

⚠️ **Auphonic gratis le mete un jingle al final del audio.** Es una marca del plan gratis, no un defecto del procesamiento: no descartarlo por eso.

- [ ] **Paso 3: Confirmar precio y política de datos de cada uno**

Precios verificados el 26-jul-2026, por grabación de 3 minutos — **confirmar que siguen vigentes**:

| Servicio | Precio | Por grabación de 3 min |
|---|---|---|
| Auphonic S ($11–13/mes por 9 h) | ~$0,024/min | $0,07 |
| Dolby.io | $0,05/min | $0,15 |
| ElevenLabs (1.000 créditos por minuto de audio) | ~$0,15–0,22/min | $0,45–0,66 |

De cada uno anotar además si **entrena modelos con lo que se le manda** (si lo hace, queda descartado por la sección 5 del spec) y si borra el archivo al terminar.

**Ninguno de los tres corta muletillas** ("eeeh", "o sea"): eso exige transcribir el audio y está fuera del primer entregable a propósito. Los silencios muertos sí desaparecen solos, porque la pieza se arma frase por frase (Tarea 2). No descartar un servicio por no cortar muletillas — ninguno lo hace.

- [ ] **Paso 4: Escuchar y decidir**

Escuchar los cuatro archivos con audífonos. Criterios, en orden: (1) la voz suena natural, no metálica ni submarina; (2) desapareció el ruido de fondo; (3) el volumen quedó parejo. El precio decide solo si hay empate.

- [ ] **Paso 5: Escribir la decisión en el spec**

Reemplazar en `docs/superpowers/specs/2026-07-26-mi-voz-design.md`, sección 9, la línea *"Servicio externo de pulido — a definir en la prueba del punto 7"* por el servicio elegido, su precio por minuto y la fecha de la prueba.

```bash
git add docs/superpowers/specs/2026-07-26-mi-voz-design.md
git commit -m "Fijar el servicio de pulido tras la prueba con voz real"
```

**Si ninguno convence:** parar el proyecto y avisar. No seguir construyendo sobre un cimiento que no sostiene.

---

## Tarea 1: Andamiaje, entorno y paquetes de afirmaciones

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `.gitignore`, `.env.example`
- Create: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css`
- Create: `src/lib/entorno.ts`, `src/lib/afirmaciones/paquetes.ts`
- Test: `src/lib/entorno.test.ts`, `src/lib/afirmaciones/paquetes.test.ts`

**Interfaces:**
- Consumes: nada (primera tarea).
- Produces: `leerEntorno(nombre: string): string | undefined`, `exigirEntorno(nombre: string): string`, `PAQUETES: Paquete[]`, `type Paquete = { id: string; nombre: string; descripcion: string; frases: string[] }`, `buscarPaquete(id: string): Paquete | undefined`.

- [ ] **Paso 1: Crear el proyecto**

```bash
cd C:\Users\RodrigoWork\Desktop\mi-voz
npx create-next-app@16.2.11 . --typescript --tailwind --app --src-dir --no-eslint --import-alias "@/*" --use-npm
npm pkg set scripts.test="node --test --experimental-strip-types \"src/**/*.test.ts\""
npm install @supabase/ssr@^0.12.3 @supabase/supabase-js@^2.110.8 zod@^4.4.3 server-only@^0.0.1
```

Si `create-next-app` pregunta por Turbopack, aceptar el valor por defecto.

- [ ] **Paso 2: Escribir la prueba del lector de entorno**

Crear `src/lib/entorno.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { limpiarValor } from "./entorno.ts";

test("quita el BOM que mete PowerShell al cargar variables a Vercel", () => {
  assert.equal(limpiarValor("\uFEFFhttps://ejemplo.supabase.co"), "https://ejemplo.supabase.co");
});

test("quita espacios y comillas sobrantes", () => {
  assert.equal(limpiarValor('  "abc123"  '), "abc123");
  assert.equal(limpiarValor("'abc123'"), "abc123");
});

test("deja intacto un valor limpio", () => {
  assert.equal(limpiarValor("abc123"), "abc123");
});

test("un valor vacío se trata como ausente", () => {
  assert.equal(limpiarValor("   "), "");
});
```

- [ ] **Paso 3: Correr la prueba y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./entorno.ts`.

- [ ] **Paso 4: Escribir el lector de entorno**

Crear `src/lib/entorno.ts`:

```ts
/**
 * Lectura de variables de entorno a prueba de BOM.
 *
 * Cargar variables a Vercel desde PowerShell les pega un BOM invisible
 * (\uFEFF) al inicio. El valor se ve idéntico en el panel, pero Supabase
 * falla SOLO en producción con "TypeError ... ByteString ... 65279".
 * Por eso nadie lee process.env directo en este proyecto.
 */
export function limpiarValor(bruto: string): string {
  return bruto
    .replace(/^\uFEFF/, "")
    .trim()
    .replace(/^["'](.*)["']$/s, "$1")
    .trim();
}

export function leerEntorno(nombre: string): string | undefined {
  const bruto = process.env[nombre];
  if (bruto === undefined) return undefined;
  const limpio = limpiarValor(bruto);
  return limpio === "" ? undefined : limpio;
}

export function exigirEntorno(nombre: string): string {
  const valor = leerEntorno(nombre);
  if (!valor) throw new Error(`Falta la variable de entorno ${nombre}`);
  return valor;
}
```

- [ ] **Paso 5: Correr la prueba y ver que pasa**

Run: `npm test`
Expected: PASS, 4 pruebas.

- [ ] **Paso 6: Escribir la prueba de los paquetes de afirmaciones**

Crear `src/lib/afirmaciones/paquetes.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { PAQUETES, buscarPaquete } from "./paquetes.ts";

test("hay cinco paquetes con identificadores únicos", () => {
  assert.equal(PAQUETES.length, 5);
  const ids = new Set(PAQUETES.map((p) => p.id));
  assert.equal(ids.size, 5);
});

test("cada paquete tiene entre 5 y 12 frases", () => {
  for (const p of PAQUETES) {
    assert.ok(p.frases.length >= 5, `${p.id} tiene muy pocas frases`);
    assert.ok(p.frases.length <= 12, `${p.id} tiene demasiadas frases`);
  }
});

test("las frases están en primera persona y no quedan vacías", () => {
  for (const p of PAQUETES) {
    for (const f of p.frases) {
      assert.ok(f.trim().length > 0, `${p.id} tiene una frase vacía`);
      assert.ok(f.length <= 120, `frase muy larga en ${p.id}: ${f}`);
    }
  }
});

test("buscarPaquete encuentra por id y devuelve undefined si no existe", () => {
  assert.equal(buscarPaquete("calma")?.id, "calma");
  assert.equal(buscarPaquete("no-existe"), undefined);
});
```

- [ ] **Paso 7: Correr y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./paquetes.ts`.

- [ ] **Paso 8: Escribir los paquetes**

Crear `src/lib/afirmaciones/paquetes.ts`:

```ts
export type Paquete = {
  id: string;
  nombre: string;
  descripcion: string;
  frases: string[];
};

/**
 * Los paquetes viven en código, no en base de datos: son cinco listas de
 * texto que cambian cada varios meses. Una tabla para esto sería trabajo
 * de mantenimiento sin beneficio.
 */
export const PAQUETES: Paquete[] = [
  {
    id: "autoestima",
    nombre: "Autoestima",
    descripcion: "Para los días en que te cuesta reconocerte.",
    frases: [
      "Merezco el mismo cariño que doy.",
      "Mi valor no depende de lo que logré hoy.",
      "Estoy aprendiendo, y eso ya es avanzar.",
      "Puedo equivocarme sin dejar de quererme.",
      "Confío en lo que soy capaz de sostener.",
      "Hablo conmigo con respeto.",
    ],
  },
  {
    id: "calma",
    nombre: "Calma",
    descripcion: "Para bajar las revoluciones.",
    frases: [
      "Respiro, y con eso basta por ahora.",
      "Lo que siento va a pasar, como pasa todo.",
      "No tengo que resolverlo todo hoy.",
      "Suelto lo que no está en mis manos.",
      "Mi cuerpo sabe descansar.",
      "Estoy a salvo en este momento.",
    ],
  },
  {
    id: "abundancia",
    nombre: "Abundancia",
    descripcion: "Para abrir la puerta a lo que quieres construir.",
    frases: [
      "Lo que construyo con cuidado da fruto.",
      "Recibo con las manos abiertas.",
      "Hay lugar para mí en lo que deseo.",
      "Mi trabajo tiene valor y lo sé cobrar.",
      "Confío en que lo que necesito llega.",
      "Comparto sin miedo a que me falte.",
    ],
  },
  {
    id: "dormir",
    nombre: "Dormir",
    descripcion: "Para soltar el día.",
    frases: [
      "El día ya terminó, y lo hice bien.",
      "Dejo los pendientes esperando hasta mañana.",
      "Mi cuerpo pesa y se hunde tranquilo.",
      "No tengo nada que resolver esta noche.",
      "Me entrego al sueño sin apuro.",
      "Mañana habrá tiempo.",
    ],
  },
  {
    id: "manana",
    nombre: "Empezar el día",
    descripcion: "Para arrancar con otro pie.",
    frases: [
      "Hoy empiezo de nuevo.",
      "Tengo la energía que este día me pide.",
      "Voy a mi ritmo y llego.",
      "Elijo cómo quiero estar hoy.",
      "Lo que hago hoy cuenta.",
      "Salgo con la cabeza en alto.",
    ],
  },
];

export function buscarPaquete(id: string): Paquete | undefined {
  return PAQUETES.find((p) => p.id === id);
}
```

- [ ] **Paso 9: Correr las pruebas y ver que pasan**

Run: `npm test`
Expected: PASS, 8 pruebas.

- [ ] **Paso 10: Crear el ejemplo de variables de entorno**

Crear `.env.example`:

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PULIDO_API_KEY=
PULIDO_PROVEEDOR=
```

- [ ] **Paso 11: Verificar que compila**

Run: `npm run build`
Expected: build exitoso.

- [ ] **Paso 12: Commit**

```bash
git add package.json package-lock.json tsconfig.json next.config.ts postcss.config.mjs .gitignore .env.example src
git commit -m "Andamiaje del proyecto, lector de entorno sin BOM y paquetes de afirmaciones"
```

---

## Tarea 2: El planificador de mezcla

Es el núcleo del producto y la pieza con más riesgo de errores sutiles. Por eso es código puro —sin audio, sin navegador, sin React— que produce una estructura de datos verificable.

**Files:**
- Create: `src/lib/audio/plan.ts`
- Test: `src/lib/audio/plan.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `type Frase = { inicio: number; fin: number }` (segundos dentro de la voz master)
  - `type AjustesAudio = { fondo: string; gananciaFondo: number; pausaSeg: number; orden: "original" | "barajado" }`
  - `type BloqueVoz = { frase: number; entraEn: number; desde: number; hasta: number }`
  - `type PlanDeMezcla = { duracionTotal: number; voz: BloqueVoz[]; fondo: { pista: string; ganancia: number; entrada: number; salida: number } }`
  - `construirPlan(frases: Frase[], ajustes: AjustesAudio, permutacion?: number[]): PlanDeMezcla`
  - Constantes `ENTRADA_FONDO = 2`, `SALIDA_FONDO = 3`.

- [ ] **Paso 1: Escribir las pruebas**

Crear `src/lib/audio/plan.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { construirPlan, ENTRADA_FONDO, SALIDA_FONDO } from "./plan.ts";
import type { AjustesAudio, Frase } from "./plan.ts";

const FRASES: Frase[] = [
  { inicio: 0.0, fin: 4.0 },   // 4 s
  { inicio: 5.2, fin: 8.2 },   // 3 s
  { inicio: 9.0, fin: 11.0 },  // 2 s
];

const AJUSTES: AjustesAudio = {
  fondo: "lluvia",
  gananciaFondo: 0.35,
  pausaSeg: 2,
  orden: "original",
};

test("la voz no arranca en cero: el fondo entra primero", () => {
  const plan = construirPlan(FRASES, AJUSTES);
  assert.equal(plan.voz[0].entraEn, ENTRADA_FONDO);
});

test("respeta exactamente la pausa pedida entre frases", () => {
  const plan = construirPlan(FRASES, AJUSTES);
  for (let i = 1; i < plan.voz.length; i++) {
    const anterior = plan.voz[i - 1];
    const finAnterior = anterior.entraEn + (anterior.hasta - anterior.desde);
    assert.equal(
      Number((plan.voz[i].entraEn - finAnterior).toFixed(6)),
      AJUSTES.pausaSeg,
    );
  }
});

test("dos frases nunca se pisan", () => {
  const plan = construirPlan(FRASES, { ...AJUSTES, pausaSeg: 0 });
  for (let i = 1; i < plan.voz.length; i++) {
    const anterior = plan.voz[i - 1];
    const finAnterior = anterior.entraEn + (anterior.hasta - anterior.desde);
    assert.ok(plan.voz[i].entraEn >= finAnterior, `la frase ${i} se pisa con la anterior`);
  }
});

test("la duración total cuadra: entrada + frases + pausas + salida", () => {
  const plan = construirPlan(FRASES, AJUSTES);
  const suma = 4 + 3 + 2;
  const pausas = AJUSTES.pausaSeg * (FRASES.length - 1);
  assert.equal(plan.duracionTotal, ENTRADA_FONDO + suma + pausas + SALIDA_FONDO);
});

test("cada bloque conserva el tramo exacto de la voz master", () => {
  const plan = construirPlan(FRASES, AJUSTES);
  plan.voz.forEach((b, i) => {
    assert.equal(b.desde, FRASES[i].inicio);
    assert.equal(b.hasta, FRASES[i].fin);
  });
});

test("el fondo usa la pista, la ganancia y los desvanecidos pedidos", () => {
  const plan = construirPlan(FRASES, AJUSTES);
  assert.equal(plan.fondo.pista, "lluvia");
  assert.equal(plan.fondo.ganancia, 0.35);
  assert.equal(plan.fondo.entrada, ENTRADA_FONDO);
  assert.equal(plan.fondo.salida, SALIDA_FONDO);
});

test("con orden barajado usa la permutación recibida", () => {
  const plan = construirPlan(FRASES, { ...AJUSTES, orden: "barajado" }, [2, 0, 1]);
  assert.deepEqual(plan.voz.map((b) => b.frase), [2, 0, 1]);
  assert.equal(plan.voz[0].desde, 9.0);
});

test("una sola frase no genera pausas", () => {
  const plan = construirPlan([{ inicio: 1, fin: 3 }], AJUSTES);
  assert.equal(plan.voz.length, 1);
  assert.equal(plan.duracionTotal, ENTRADA_FONDO + 2 + SALIDA_FONDO);
});

test("sin frases devuelve un plan vacío pero válido", () => {
  const plan = construirPlan([], AJUSTES);
  assert.deepEqual(plan.voz, []);
  assert.equal(plan.duracionTotal, ENTRADA_FONDO + SALIDA_FONDO);
});

test("rechaza ganancias fuera de rango", () => {
  assert.throws(() => construirPlan(FRASES, { ...AJUSTES, gananciaFondo: 1.5 }));
  assert.throws(() => construirPlan(FRASES, { ...AJUSTES, gananciaFondo: -0.1 }));
});

test("rechaza pausas negativas", () => {
  assert.throws(() => construirPlan(FRASES, { ...AJUSTES, pausaSeg: -1 }));
});

test("rechaza frases con fin anterior al inicio", () => {
  assert.throws(() => construirPlan([{ inicio: 5, fin: 2 }], AJUSTES));
});
```

- [ ] **Paso 2: Correr y ver que fallan**

Run: `npm test`
Expected: FAIL — no existe `./plan.ts`.

- [ ] **Paso 3: Escribir el planificador**

Crear `src/lib/audio/plan.ts`:

```ts
/**
 * Planificador de mezcla.
 *
 * No suena nada aquí: esto solo decide QUÉ suena, CUÁNDO y CON QUÉ volumen,
 * y lo devuelve como datos. El reproductor (reproductor.ts) ejecuta ese plan.
 *
 * La razón de separarlo: los errores de audio son horribles de cazar oyendo
 * —una pausa de más, dos frases pisadas, un fade que corta la última palabra—.
 * Como plan es una estructura de datos, esos errores se cazan leyendo.
 */

export type Frase = { inicio: number; fin: number };

export type AjustesAudio = {
  fondo: string;
  gananciaFondo: number;
  pausaSeg: number;
  orden: "original" | "barajado";
};

export type BloqueVoz = {
  frase: number;
  entraEn: number;
  desde: number;
  hasta: number;
};

export type PlanDeMezcla = {
  duracionTotal: number;
  voz: BloqueVoz[];
  fondo: { pista: string; ganancia: number; entrada: number; salida: number };
};

/** Segundos de fondo solo, antes de la primera palabra. */
export const ENTRADA_FONDO = 2;
/** Segundos de fondo solo, después de la última palabra. */
export const SALIDA_FONDO = 3;

export function construirPlan(
  frases: Frase[],
  ajustes: AjustesAudio,
  permutacion?: number[],
): PlanDeMezcla {
  if (ajustes.gananciaFondo < 0 || ajustes.gananciaFondo > 1) {
    throw new Error("La ganancia del fondo debe estar entre 0 y 1");
  }
  if (ajustes.pausaSeg < 0) {
    throw new Error("La pausa no puede ser negativa");
  }
  for (const f of frases) {
    if (f.fin <= f.inicio) {
      throw new Error(`Frase inválida: fin ${f.fin} no es posterior a inicio ${f.inicio}`);
    }
  }

  const indices =
    ajustes.orden === "barajado" && permutacion
      ? permutacion
      : frases.map((_, i) => i);

  const voz: BloqueVoz[] = [];
  let reloj = ENTRADA_FONDO;

  indices.forEach((indice, posicion) => {
    const f = frases[indice];
    if (posicion > 0) reloj += ajustes.pausaSeg;
    voz.push({ frase: indice, entraEn: redondear(reloj), desde: f.inicio, hasta: f.fin });
    reloj += f.fin - f.inicio;
  });

  return {
    duracionTotal: redondear(reloj + SALIDA_FONDO),
    voz,
    fondo: {
      pista: ajustes.fondo,
      ganancia: ajustes.gananciaFondo,
      entrada: ENTRADA_FONDO,
      salida: SALIDA_FONDO,
    },
  };
}

/** Evita que la suma de decimales arrastre errores de coma flotante. */
function redondear(n: number): number {
  return Number(n.toFixed(6));
}
```

- [ ] **Paso 4: Correr y ver que pasan**

Run: `npm test`
Expected: PASS, 20 pruebas en total.

- [ ] **Paso 5: Commit**

```bash
git add src/lib/audio/plan.ts src/lib/audio/plan.test.ts
git commit -m "Planificador de mezcla: decide que suena, cuando y con que volumen"
```

---

## Tarea 3: Detección de frases por silencios

**Files:**
- Create: `src/lib/audio/frases.ts`
- Test: `src/lib/audio/frases.test.ts`

**Interfaces:**
- Consumes: `type Frase` de `@/lib/audio/plan`.
- Produces:
  - `calcularEnergia(muestras: Float32Array, muestrasPorVentana: number): number[]`
  - `detectarFrases(energia: number[], segPorVentana: number, opciones?: OpcionesCorte): Frase[]`
  - `type OpcionesCorte = { silencioMinSeg?: number; fraseMinSeg?: number; margenSeg?: number; umbralRelativo?: number }`

- [ ] **Paso 1: Escribir las pruebas**

Crear `src/lib/audio/frases.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { calcularEnergia, detectarFrases } from "./frases.ts";

/** Construye una pista de energía: bloques de voz (alta) y silencio (baja). */
function pista(tramos: Array<{ ventanas: number; nivel: number }>): number[] {
  const salida: number[] = [];
  for (const t of tramos) for (let i = 0; i < t.ventanas; i++) salida.push(t.nivel);
  return salida;
}

const SEG_POR_VENTANA = 0.02; // ventanas de 20 ms

test("calcularEnergia promedia por ventana", () => {
  const muestras = new Float32Array([1, 1, 0, 0, -1, -1]);
  const energia = calcularEnergia(muestras, 2);
  assert.equal(energia.length, 3);
  assert.equal(energia[0], 1);
  assert.equal(energia[1], 0);
  assert.equal(energia[2], 1);
});

test("calcularEnergia descarta la ventana final incompleta", () => {
  const muestras = new Float32Array([1, 1, 1]);
  assert.equal(calcularEnergia(muestras, 2).length, 1);
});

test("separa dos frases con un silencio largo en medio", () => {
  const energia = pista([
    { ventanas: 50, nivel: 0.5 },   // 1 s de voz
    { ventanas: 30, nivel: 0.001 }, // 600 ms de silencio
    { ventanas: 50, nivel: 0.5 },   // 1 s de voz
  ]);
  const frases = detectarFrases(energia, SEG_POR_VENTANA);
  assert.equal(frases.length, 2);
});

test("no parte por una pausa corta de respiración", () => {
  const energia = pista([
    { ventanas: 50, nivel: 0.5 },
    { ventanas: 8, nivel: 0.001 },  // 160 ms: menos del silencio mínimo
    { ventanas: 50, nivel: 0.5 },
  ]);
  assert.equal(detectarFrases(energia, SEG_POR_VENTANA).length, 1);
});

test("descarta chasquidos más cortos que la frase mínima", () => {
  const energia = pista([
    { ventanas: 3, nivel: 0.5 },    // 60 ms: un ruidito
    { ventanas: 30, nivel: 0.001 },
    { ventanas: 50, nivel: 0.5 },
  ]);
  const frases = detectarFrases(energia, SEG_POR_VENTANA);
  assert.equal(frases.length, 1);
});

test("deja margen antes y después para no cortar la respiración", () => {
  const energia = pista([
    { ventanas: 25, nivel: 0.001 }, // 500 ms de silencio inicial
    { ventanas: 50, nivel: 0.5 },
    { ventanas: 25, nivel: 0.001 },
  ]);
  const [frase] = detectarFrases(energia, SEG_POR_VENTANA, { margenSeg: 0.06 });
  assert.ok(frase.inicio < 25 * SEG_POR_VENTANA, "debió adelantar el inicio");
  assert.ok(frase.inicio >= 0);
  assert.ok(frase.fin > 75 * SEG_POR_VENTANA, "debió atrasar el fin");
});

test("las frases salen ordenadas y sin solaparse", () => {
  const energia = pista([
    { ventanas: 40, nivel: 0.4 },
    { ventanas: 30, nivel: 0.001 },
    { ventanas: 40, nivel: 0.6 },
    { ventanas: 30, nivel: 0.001 },
    { ventanas: 40, nivel: 0.5 },
  ]);
  const frases = detectarFrases(energia, SEG_POR_VENTANA);
  assert.equal(frases.length, 3);
  for (let i = 1; i < frases.length; i++) {
    assert.ok(frases[i].inicio >= frases[i - 1].fin, "las frases se solapan");
  }
});

test("silencio total no devuelve ninguna frase", () => {
  const energia = pista([{ ventanas: 100, nivel: 0 }]);
  assert.deepEqual(detectarFrases(energia, SEG_POR_VENTANA), []);
});

test("el umbral es relativo: una grabación bajita también se corta bien", () => {
  const fuerte = pista([
    { ventanas: 50, nivel: 0.8 },
    { ventanas: 30, nivel: 0.002 },
    { ventanas: 50, nivel: 0.8 },
  ]);
  const bajita = pista([
    { ventanas: 50, nivel: 0.05 },
    { ventanas: 30, nivel: 0.0001 },
    { ventanas: 50, nivel: 0.05 },
  ]);
  assert.equal(detectarFrases(fuerte, SEG_POR_VENTANA).length, 2);
  assert.equal(detectarFrases(bajita, SEG_POR_VENTANA).length, 2);
});
```

- [ ] **Paso 2: Correr y ver que fallan**

Run: `npm test`
Expected: FAIL — no existe `./frases.ts`.

- [ ] **Paso 3: Escribir el detector**

Crear `src/lib/audio/frases.ts`:

```ts
import type { Frase } from "./plan.ts";

/**
 * Corte de la voz master en frases.
 *
 * Corre en el navegador después del pulido, cuando el audio ya viene limpio
 * y los silencios son silencios de verdad. Se separó del navegador a
 * propósito: recibe números y devuelve números, así que se puede probar sin
 * reproducir nada.
 *
 * El umbral es RELATIVO al pico de la propia grabación. Un umbral fijo
 * funcionaría solo con quien graba fuerte; quien habla bajito quedaría como
 * silencio de punta a punta.
 */

export type OpcionesCorte = {
  /** Silencio mínimo para considerar que terminó una frase. */
  silencioMinSeg?: number;
  /** Duración mínima para que un tramo con sonido cuente como frase. */
  fraseMinSeg?: number;
  /** Colchón que se agrega antes y después de cada frase. */
  margenSeg?: number;
  /** Fracción del pico por debajo de la cual se considera silencio. */
  umbralRelativo?: number;
};

export function calcularEnergia(
  muestras: Float32Array,
  muestrasPorVentana: number,
): number[] {
  const ventanas: number[] = [];
  const total = Math.floor(muestras.length / muestrasPorVentana);
  for (let v = 0; v < total; v++) {
    let suma = 0;
    const desde = v * muestrasPorVentana;
    for (let i = desde; i < desde + muestrasPorVentana; i++) {
      suma += muestras[i] * muestras[i];
    }
    ventanas.push(Math.sqrt(suma / muestrasPorVentana));
  }
  return ventanas;
}

export function detectarFrases(
  energia: number[],
  segPorVentana: number,
  opciones: OpcionesCorte = {},
): Frase[] {
  const silencioMin = opciones.silencioMinSeg ?? 0.35;
  const fraseMin = opciones.fraseMinSeg ?? 0.3;
  const margen = opciones.margenSeg ?? 0.06;
  const umbralRelativo = opciones.umbralRelativo ?? 0.08;

  const pico = Math.max(...energia, 0);
  if (pico <= 0) return [];
  const umbral = pico * umbralRelativo;

  const ventanasSilencio = Math.ceil(silencioMin / segPorVentana);

  const crudas: Array<{ desde: number; hasta: number }> = [];
  let inicio: number | null = null;
  let silencioSeguido = 0;

  energia.forEach((valor, i) => {
    if (valor > umbral) {
      if (inicio === null) inicio = i;
      silencioSeguido = 0;
    } else if (inicio !== null) {
      silencioSeguido++;
      if (silencioSeguido >= ventanasSilencio) {
        crudas.push({ desde: inicio, hasta: i - silencioSeguido + 1 });
        inicio = null;
        silencioSeguido = 0;
      }
    }
  });
  if (inicio !== null) {
    crudas.push({ desde: inicio, hasta: energia.length });
  }

  const finPista = energia.length * segPorVentana;

  return crudas
    .filter((t) => (t.hasta - t.desde) * segPorVentana >= fraseMin)
    .map((t) => ({
      inicio: Math.max(0, t.desde * segPorVentana - margen),
      fin: Math.min(finPista, t.hasta * segPorVentana + margen),
    }));
}
```

- [ ] **Paso 4: Correr y ver que pasan**

Run: `npm test`
Expected: PASS, 29 pruebas en total.

- [ ] **Paso 5: Commit**

```bash
git add src/lib/audio/frases.ts src/lib/audio/frases.test.ts
git commit -m "Detectar frases por silencios con umbral relativo al pico"
```

---

## Tarea 4: Base de datos y clientes de Supabase

**Files:**
- Create: `supabase/migrations/0001_esquema_voz.sql`, `supabase/migrations/0002_rls_voz.sql`, `supabase/migrations/0003_almacen_voces.sql`
- Create: `src/lib/supabase/esquema.ts`, `src/lib/supabase/servidor.ts`, `src/lib/supabase/cliente.ts`, `src/lib/supabase/admin.ts`
- Create: `src/proxy.ts`

⚠️ **Corrección sobre el plan original:** en Next 16 el archivo `middleware.ts` está deprecado y se llama **`proxy.ts`**, exportando una función `proxy` (no `middleware`). Verificado en `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`. Un `middleware.ts` en Next 16 simplemente no se ejecuta, y las pantallas privadas quedarían abiertas sin que nada avise.

**Interfaces:**
- Consumes: `exigirEntorno` de `@/lib/entorno`.
- Produces: `SCHEMA = "voz"`, `supabaseServidor()`, `supabaseNavegador()`, `supabaseAdmin()`. Tablas `voz.perfiles`, `voz.textos`, `voz.grabaciones`, `voz.ajustes_audio`. Bucket privado `voces`.

- [ ] **Paso 1: Escribir la migración del schema**

Crear `supabase/migrations/0001_esquema_voz.sql`:

```sql
-- Schema propio dentro de mandarina-DATA, junto a crm, inbox, mata y rrhh.
create schema if not exists voz;

-- Perfil de la persona. El id es el mismo de auth.users: el login (celular
-- + código por WhatsApp) es el que ya existe en este proyecto.
create table voz.perfiles (
  id uuid primary key references auth.users(id) on delete cascade,
  celular text not null,
  plan text not null default 'gratis' check (plan in ('gratis', 'pago')),
  consentimiento_en timestamptz,
  creado_en timestamptz not null default now()
);

-- El texto que la persona va a leer. Puede venir de un paquete y luego
-- editarse; guardamos el resultado final, no la referencia al paquete.
create table voz.textos (
  id uuid primary key default gen_random_uuid(),
  perfil_id uuid not null references voz.perfiles(id) on delete cascade,
  nombre text not null,
  frases text[] not null check (array_length(frases, 1) between 1 and 12),
  creado_en timestamptz not null default now()
);

-- Una grabación = una lectura + su pulido. Es el activo caro: se paga una
-- sola vez y de ella salen todos los audios que la persona arme después.
create table voz.grabaciones (
  id uuid primary key default gen_random_uuid(),
  perfil_id uuid not null references voz.perfiles(id) on delete cascade,
  texto_id uuid not null references voz.textos(id) on delete cascade,
  estado text not null default 'cruda'
    check (estado in ('cruda', 'puliendo', 'lista', 'fallida')),
  ruta_cruda text,
  ruta_master text,
  duracion_seg numeric,
  cortes jsonb,
  error text,
  creado_en timestamptz not null default now(),
  actualizado_en timestamptz not null default now()
);

create index grabaciones_por_perfil on voz.grabaciones (perfil_id, creado_en desc);

-- Configuraciones guardadas: fondo, volumen y pausas. Livianas a propósito
-- (no guardan audio), por eso pueden ser ilimitadas incluso en el plan gratis.
create table voz.ajustes_audio (
  id uuid primary key default gen_random_uuid(),
  perfil_id uuid not null references voz.perfiles(id) on delete cascade,
  grabacion_id uuid not null references voz.grabaciones(id) on delete cascade,
  nombre text not null,
  fondo text not null,
  ganancia_fondo numeric not null check (ganancia_fondo between 0 and 1),
  pausa_seg numeric not null check (pausa_seg >= 0),
  orden text not null default 'original' check (orden in ('original', 'barajado')),
  creado_en timestamptz not null default now()
);
```

- [ ] **Paso 2: Escribir la migración de RLS**

Crear `supabase/migrations/0002_rls_voz.sql`:

```sql
-- Sin esto, cualquier persona con la clave anónima podría leer las voces y
-- los textos de todas las demás. La clave anónima está en el navegador.
alter table voz.perfiles      enable row level security;
alter table voz.textos        enable row level security;
alter table voz.grabaciones   enable row level security;
alter table voz.ajustes_audio enable row level security;

create policy "cada quien ve su perfil" on voz.perfiles
  for select using (auth.uid() = id);
create policy "cada quien edita su perfil" on voz.perfiles
  for update using (auth.uid() = id);
create policy "cada quien crea su perfil" on voz.perfiles
  for insert with check (auth.uid() = id);

create policy "textos propios" on voz.textos
  for all using (auth.uid() = perfil_id) with check (auth.uid() = perfil_id);

create policy "grabaciones propias" on voz.grabaciones
  for all using (auth.uid() = perfil_id) with check (auth.uid() = perfil_id);

create policy "ajustes propios" on voz.ajustes_audio
  for all using (auth.uid() = perfil_id) with check (auth.uid() = perfil_id);

-- PostgREST no expone un schema nuevo por sí solo: sin esto la API responde
-- 406 "Invalid schema". Ya pasó con el schema rrhh.
grant usage on schema voz to anon, authenticated, service_role;
grant all on all tables in schema voz to authenticated, service_role;
alter default privileges in schema voz
  grant all on tables to authenticated, service_role;
```

- [ ] **Paso 3: Escribir la migración del almacén**

Crear `supabase/migrations/0003_almacen_voces.sql`:

```sql
-- Bucket PRIVADO. Nada de public: la voz de una persona no se sirve por una
-- URL adivinable. Cada reproducción pide una URL firmada de corta vida.
insert into storage.buckets (id, name, public)
values ('voces', 'voces', false)
on conflict (id) do nothing;

-- Cada persona solo toca su propia carpeta: voces/<su uuid>/...
create policy "sube a su carpeta" on storage.objects
  for insert to authenticated
  with check (bucket_id = 'voces' and (storage.foldername(name))[1] = auth.uid()::text);

create policy "lee su carpeta" on storage.objects
  for select to authenticated
  using (bucket_id = 'voces' and (storage.foldername(name))[1] = auth.uid()::text);

create policy "borra su carpeta" on storage.objects
  for delete to authenticated
  using (bucket_id = 'voces' and (storage.foldername(name))[1] = auth.uid()::text);
```

- [ ] **Paso 4: Aplicar las migraciones**

Aplicarlas contra `mandarina-DATA` (`piingkecjgoisnxccvaa`), una por una y en orden.

Después, en el panel de Supabase: **Settings → API → Exposed schemas**, agregar `voz` a la lista. Sin este paso la API responde `406 Invalid schema` y nada funciona.

Verificar:

```sql
select table_name from information_schema.tables where table_schema = 'voz';
```

Expected: `perfiles`, `textos`, `grabaciones`, `ajustes_audio`.

- [ ] **Paso 5: Escribir los clientes de Supabase**

Crear `src/lib/supabase/esquema.ts`:

```ts
/**
 * Schema propio dentro del proyecto Supabase compartido (convive con
 * `crm`, `inbox`, `mata` y `rrhh`).
 *
 * En archivo aparte para que lo importen el cliente del navegador y el del
 * servidor sin arrastrar `next/headers` al bundle del navegador.
 */
export const SCHEMA = "voz";
```

Crear `src/lib/supabase/servidor.ts`:

```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";
import { exigirEntorno } from "@/lib/entorno";
import { SCHEMA } from "./esquema";

export { SCHEMA };

/** Cliente para Server Components, Server Actions y Route Handlers. Pasa por RLS. */
export async function supabaseServidor() {
  const almacen = await cookies();

  return createServerClient(
    exigirEntorno("NEXT_PUBLIC_SUPABASE_URL"),
    exigirEntorno("NEXT_PUBLIC_SUPABASE_ANON_KEY"),
    {
      db: { schema: SCHEMA },
      cookies: {
        getAll() {
          return almacen.getAll();
        },
        setAll(cookiesNuevas) {
          try {
            cookiesNuevas.forEach(({ name, value, options }) =>
              almacen.set(name, value, options),
            );
          } catch {
            // Los Server Components no pueden escribir cookies; el refresco
            // de sesión lo hace el middleware.
          }
        },
      },
    },
  );
}
```

Crear `src/lib/supabase/cliente.ts`:

```ts
"use client";
import { createBrowserClient } from "@supabase/ssr";
import { SCHEMA } from "./esquema";

export function supabaseNavegador() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { db: { schema: SCHEMA } },
  );
}
```

Crear `src/lib/supabase/admin.ts`:

```ts
import "server-only";
import { createClient } from "@supabase/supabase-js";
import { exigirEntorno } from "@/lib/entorno";
import { SCHEMA } from "./esquema";

/**
 * Cliente con service role: SALTA RLS. Solo para el pulido, que actualiza
 * grabaciones en nombre de la persona. Nunca se importa desde un componente
 * de cliente.
 */
export function supabaseAdmin() {
  return createClient(
    exigirEntorno("NEXT_PUBLIC_SUPABASE_URL"),
    exigirEntorno("SUPABASE_SERVICE_ROLE_KEY"),
    { db: { schema: SCHEMA }, auth: { persistSession: false } },
  );
}
```

- [ ] **Paso 6: Escribir el proxy**

Crear `src/proxy.ts` (**no** `middleware.ts`):

```ts
import { createServerClient } from "@supabase/ssr";
import { NextResponse, type NextRequest } from "next/server";

/** Refresca la sesión y cierra el paso a las pantallas privadas. */
export async function proxy(peticion: NextRequest) {
  let respuesta = NextResponse.next({ request: peticion });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return peticion.cookies.getAll();
        },
        setAll(cookiesNuevas) {
          cookiesNuevas.forEach(({ name, value }) => peticion.cookies.set(name, value));
          respuesta = NextResponse.next({ request: peticion });
          cookiesNuevas.forEach(({ name, value, options }) =>
            respuesta.cookies.set(name, value, options),
          );
        },
      },
    },
  );

  const { data: { user } } = await supabase.auth.getUser();
  const privada = /^\/(estudio|grabar|mezclar|cuenta)/.test(peticion.nextUrl.pathname);

  if (privada && !user) {
    const destino = peticion.nextUrl.clone();
    destino.pathname = "/ingresar";
    return NextResponse.redirect(destino);
  }

  return respuesta;
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico|fondos|.*\\.mp3).*)"],
};
```

- [ ] **Paso 7: Verificar que compila**

Run: `npm run build`
Expected: build exitoso.

- [ ] **Paso 8: Commit**

```bash
git add supabase/migrations src/lib/supabase src/proxy.ts
git commit -m "Schema voz, RLS, almacen privado de voces y clientes de Supabase"
```

⚠️ **PENDIENTE ABIERTO (26-jul):** falta agregar `voz` a los schemas expuestos. Hoy la lista es
`public, graphql_public, crm, inbox, rrhh, mata`. Hasta hacerlo, la API responde `406 Invalid
schema` y la app no se puede probar de punta a punta. Se hace en Supabase → Settings → API →
Exposed schemas, o por SQL:

```sql
alter role authenticator set pgrst.db_schemas = 'public, graphql_public, crm, inbox, rrhh, mata, voz';
notify pgrst, 'reload config';
```

Esa lista la comparten el CRM, los inboxes, RRHH y La Mata: si se escribe mal, se cae la API de
los cuatro.

---

## Tarea 5: Entrar con celular y código por WhatsApp

Se reusa tal cual el mecanismo de `lamata-premios`: Supabase Auth genera el código y el hook `Send SMS` se lo entrega a la Edge Function `enviar-otp-whatsapp`, que lo manda con la plantilla de autenticación `codigo_acceso`.

**El hook ya está configurado a nivel de proyecto en `mandarina-DATA`**, así que esta tarea no despliega ninguna Edge Function: solo construye las pantallas. Consecuencia a tener presente: los usuarios de `auth.users` son los mismos de La Mata; el aislamiento entre productos lo da la tabla `voz.perfiles`, no el login.

**Files:**
- Create: `src/lib/telefono.ts`, `src/app/(auth)/ingresar/page.tsx`, `src/app/(auth)/ingresar/acciones.ts`, `src/app/(auth)/verificar/page.tsx`, `src/app/(auth)/verificar/Formulario.tsx`, `src/app/(auth)/verificar/acciones.ts`
- Test: `src/lib/telefono.test.ts`

**Interfaces:**
- Consumes: `supabaseServidor()`.
- Produces: `normalizarCelular(bruto: string): string | null` (devuelve E.164 `+593…`), `ocultarCelular(e164: string): string`, server actions `pedirCodigo(_: unknown, datos: FormData)` y `validarCodigo(_: unknown, datos: FormData)`.

- [ ] **Paso 1: Escribir la prueba del teléfono**

Crear `src/lib/telefono.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { normalizarCelular, ocultarCelular } from "./telefono.ts";

test("acepta las formas en que la gente escribe su celular en Ecuador", () => {
  for (const entrada of ["0983745757", "983745757", "+593983745757", "593983745757", "098 374 5757"]) {
    assert.equal(normalizarCelular(entrada), "+593983745757", `falló con ${entrada}`);
  }
});

test("rechaza lo que no es un celular ecuatoriano", () => {
  for (const entrada of ["12345", "022345678", "", "abcdefghij", "09837457570"]) {
    assert.equal(normalizarCelular(entrada), null, `no debió aceptar ${entrada}`);
  }
});

test("oculta el número dejando solo los últimos dígitos", () => {
  assert.equal(ocultarCelular("+593983745757"), "••••• 5757");
});
```

- [ ] **Paso 2: Correr y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./telefono.ts`.

- [ ] **Paso 3: Escribir el normalizador**

Crear `src/lib/telefono.ts`:

```ts
/**
 * Normalización de celulares ecuatorianos a E.164.
 *
 * Supabase Auth exige el formato +593XXXXXXXXX. La gente escribe su número
 * de cinco maneras distintas, así que se normaliza antes de pedir el código:
 * un número mal formado significa un WhatsApp que nunca llega.
 */
export function normalizarCelular(bruto: string): string | null {
  const digitos = bruto.replace(/\D/g, "");

  let nacional: string;
  if (digitos.startsWith("593")) nacional = digitos.slice(3);
  else if (digitos.startsWith("0")) nacional = digitos.slice(1);
  else nacional = digitos;

  // Celular ecuatoriano: 9 dígitos empezando en 9.
  if (!/^9\d{8}$/.test(nacional)) return null;
  return `+593${nacional}`;
}

export function ocultarCelular(e164: string): string {
  return `••••• ${e164.slice(-4)}`;
}
```

- [ ] **Paso 4: Correr y ver que pasa**

Run: `npm test`
Expected: PASS, 32 pruebas en total.

- [ ] **Paso 5: Escribir la acción que pide el código**

Crear `src/app/(auth)/ingresar/acciones.ts`:

```ts
"use server";
import { redirect } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";
import { normalizarCelular } from "@/lib/telefono";

export type EstadoIngreso = { error?: string };

export async function pedirCodigo(
  _previo: EstadoIngreso,
  datos: FormData,
): Promise<EstadoIngreso> {
  const celular = normalizarCelular(String(datos.get("celular") ?? ""));
  if (!celular) return { error: "Ese número no parece un celular de Ecuador." };

  const supabase = await supabaseServidor();
  const { error } = await supabase.auth.signInWithOtp({ phone: celular });

  if (error) {
    return { error: "No pudimos enviar el código. Inténtalo de nuevo en un minuto." };
  }

  redirect(`/verificar?celular=${encodeURIComponent(celular)}`);
}
```

- [ ] **Paso 6: Escribir la pantalla de ingreso**

Crear `src/app/(auth)/ingresar/page.tsx`:

```tsx
"use client";
import { useActionState } from "react";
import { pedirCodigo, type EstadoIngreso } from "./acciones";

export default function Ingresar() {
  const [estado, accion, pendiente] = useActionState<EstadoIngreso, FormData>(
    pedirCodigo,
    {},
  );

  return (
    <main className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center gap-6 px-6">
      <div>
        <h1 className="text-3xl font-semibold">Tu voz</h1>
        <p className="mt-2 text-neutral-500">
          Te mandamos un código por WhatsApp para entrar.
        </p>
      </div>

      <form action={accion} className="flex flex-col gap-4">
        <input
          name="celular"
          type="tel"
          inputMode="numeric"
          autoComplete="tel"
          placeholder="09XXXXXXXX"
          required
          className="rounded-xl border border-neutral-300 px-4 py-3 text-lg"
        />
        {estado.error && <p className="text-sm text-red-600">{estado.error}</p>}
        <button
          type="submit"
          disabled={pendiente}
          className="rounded-xl bg-neutral-900 px-4 py-3 text-white disabled:opacity-50"
        >
          {pendiente ? "Enviando…" : "Enviarme el código"}
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Paso 7: Escribir la acción que valida el código**

Crear `src/app/(auth)/verificar/acciones.ts`:

```ts
"use server";
import { redirect } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";

export type EstadoVerificacion = { error?: string };

export async function validarCodigo(
  _previo: EstadoVerificacion,
  datos: FormData,
): Promise<EstadoVerificacion> {
  const celular = String(datos.get("celular") ?? "");
  const codigo = String(datos.get("codigo") ?? "").trim();

  if (!/^\d{6}$/.test(codigo)) return { error: "El código son 6 números." };

  const supabase = await supabaseServidor();
  const { data, error } = await supabase.auth.verifyOtp({
    phone: celular,
    token: codigo,
    type: "sms",
  });

  if (error || !data.user) return { error: "Ese código no es válido o ya venció." };

  // Alta del perfil la primera vez. Sin esto, la persona entra pero no
  // existe para la app.
  await supabase
    .from("perfiles")
    .upsert({ id: data.user.id, celular }, { onConflict: "id" });

  redirect("/estudio");
}
```

- [ ] **Paso 8: Escribir la pantalla de verificación**

Crear `src/app/(auth)/verificar/Formulario.tsx`:

```tsx
"use client";
import { useActionState } from "react";
import { validarCodigo, type EstadoVerificacion } from "./acciones";
import { ocultarCelular } from "@/lib/telefono";

export function Formulario({ celular }: { celular: string }) {
  const [estado, accion, pendiente] = useActionState<EstadoVerificacion, FormData>(
    validarCodigo,
    {},
  );

  return (
    <form action={accion} className="flex flex-col gap-4">
      <input type="hidden" name="celular" value={celular} />
      <p className="text-neutral-500">
        Mandamos el código al {ocultarCelular(celular)}
      </p>
      <input
        name="codigo"
        inputMode="numeric"
        autoComplete="one-time-code"
        maxLength={6}
        placeholder="000000"
        required
        className="rounded-xl border border-neutral-300 px-4 py-3 text-center text-2xl tracking-[0.4em]"
      />
      {estado.error && <p className="text-sm text-red-600">{estado.error}</p>}
      <button
        type="submit"
        disabled={pendiente}
        className="rounded-xl bg-neutral-900 px-4 py-3 text-white disabled:opacity-50"
      >
        {pendiente ? "Comprobando…" : "Entrar"}
      </button>
    </form>
  );
}
```

Crear `src/app/(auth)/verificar/page.tsx`:

```tsx
import { redirect } from "next/navigation";
import { Formulario } from "./Formulario";

export default async function Verificar({
  searchParams,
}: {
  searchParams: Promise<{ celular?: string }>;
}) {
  const { celular } = await searchParams;
  if (!celular) redirect("/ingresar");

  return (
    <main className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center gap-6 px-6">
      <h1 className="text-3xl font-semibold">Tu código</h1>
      <Formulario celular={celular} />
    </main>
  );
}
```

- [ ] **Paso 9: Probar con un celular real**

Run: `npm run dev`

Con `.env.local` apuntando a `mandarina-DATA`, abrir `http://localhost:3000/ingresar`, poner un celular real y verificar:
1. Llega el WhatsApp con el código en menos de 30 segundos.
2. El código entra y redirige a `/estudio` (que todavía no existe: un 404 aquí es éxito).
3. En Supabase, `voz.perfiles` tiene una fila nueva con ese celular.
4. Un código equivocado muestra "Ese código no es válido o ya venció."

Si el WhatsApp no llega, revisar los logs de la Edge Function `enviar-otp-whatsapp` en Supabase: el error casi siempre es la plantilla (`132001`) o el token de Meta.

- [ ] **Paso 10: Commit**

```bash
git add src/lib/telefono.ts src/lib/telefono.test.ts "src/app/(auth)"
git commit -m "Entrar con celular y codigo por WhatsApp reusando el hook de La Mata"
```

---

## Tarea 6: Elegir el texto y grabarlo

**Files:**
- Create: `src/lib/audio/grabacion.ts`, `src/app/(app)/estudio/page.tsx`, `src/app/(app)/estudio/acciones.ts`, `src/app/(app)/grabar/[textoId]/page.tsx`, `src/app/(app)/grabar/[textoId]/Grabador.tsx`
- Test: `src/lib/audio/grabacion.test.ts`

**Interfaces:**
- Consumes: `PAQUETES`, `buscarPaquete`, `supabaseServidor()`, `supabaseNavegador()`.
- Produces: `elegirFormato(soporta: (mime: string) => boolean): string | null` (puro, para poder probarlo sin navegador), `formatoSoportado(): string | null` (envuelve al anterior usando `MediaRecorder.isTypeSupported`), `extensionDe(mime: string): string`, server action `guardarTexto(_: unknown, datos: FormData)` que crea la fila en `voz.textos` y redirige a `/grabar/<id>`.

- [ ] **Paso 1: Escribir la prueba del formato**

Crear `src/lib/audio/grabacion.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { elegirFormato, extensionDe } from "./grabacion.ts";

test("prefiere webm/opus cuando el navegador lo soporta", () => {
  const soporta = (m: string) => m.startsWith("audio/webm");
  assert.equal(elegirFormato(soporta), "audio/webm;codecs=opus");
});

test("cae a mp4 en Safari, que no soporta webm", () => {
  const soporta = (m: string) => m.startsWith("audio/mp4");
  assert.equal(elegirFormato(soporta), "audio/mp4");
});

test("devuelve null si el navegador no soporta ninguno", () => {
  assert.equal(elegirFormato(() => false), null);
});

test("la extensión sale del mime", () => {
  assert.equal(extensionDe("audio/webm;codecs=opus"), "webm");
  assert.equal(extensionDe("audio/mp4"), "m4a");
});
```

- [ ] **Paso 2: Correr y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./grabacion.ts`.

- [ ] **Paso 3: Escribir el selector de formato**

Crear `src/lib/audio/grabacion.ts`:

```ts
/**
 * Elección del formato de grabación.
 *
 * Safari en iPhone no soporta webm; Chrome en Android sí y produce archivos
 * mucho más chicos. Sin esta negociación, MediaRecorder falla en la mitad de
 * los celulares del país.
 *
 * La función recibe el comprobador como parámetro para poder probarla sin
 * navegador.
 */
const CANDIDATOS = ["audio/webm;codecs=opus", "audio/webm", "audio/mp4"];

export function elegirFormato(soporta: (mime: string) => boolean): string | null {
  return CANDIDATOS.find(soporta) ?? null;
}

export function formatoSoportado(): string | null {
  if (typeof MediaRecorder === "undefined") return null;
  return elegirFormato((m) => MediaRecorder.isTypeSupported(m));
}

export function extensionDe(mime: string): string {
  if (mime.startsWith("audio/webm")) return "webm";
  if (mime.startsWith("audio/mp4")) return "m4a";
  return "bin";
}
```

- [ ] **Paso 4: Correr y ver que pasa**

Run: `npm test`
Expected: PASS, 36 pruebas en total.

- [ ] **Paso 5: Escribir la acción que guarda el texto**

Crear `src/app/(app)/estudio/acciones.ts`:

```ts
"use server";
import { redirect } from "next/navigation";
import { z } from "zod";
import { supabaseServidor } from "@/lib/supabase/servidor";

const Entrada = z.object({
  nombre: z.string().trim().min(1).max(60),
  frases: z.array(z.string().trim().min(1).max(120)).min(1).max(12),
});

export type EstadoTexto = { error?: string };

export async function guardarTexto(
  _previo: EstadoTexto,
  datos: FormData,
): Promise<EstadoTexto> {
  const crudo = {
    nombre: String(datos.get("nombre") ?? ""),
    frases: datos
      .getAll("frase")
      .map(String)
      .map((f) => f.trim())
      .filter((f) => f.length > 0),
  };

  const revisado = Entrada.safeParse(crudo);
  if (!revisado.success) {
    return { error: "Revisa el texto: hace falta al menos una frase y máximo doce." };
  }

  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  const { data, error } = await supabase
    .from("textos")
    .insert({ perfil_id: user.id, ...revisado.data })
    .select("id")
    .single();

  if (error || !data) return { error: "No pudimos guardar el texto. Inténtalo otra vez." };

  redirect(`/grabar/${data.id}`);
}
```

- [ ] **Paso 6: Escribir la pantalla del estudio**

Crear `src/app/(app)/estudio/page.tsx`:

```tsx
"use client";
import { useActionState, useState } from "react";
import { PAQUETES } from "@/lib/afirmaciones/paquetes";
import { guardarTexto, type EstadoTexto } from "./acciones";

export default function Estudio() {
  const [paquete, setPaquete] = useState(PAQUETES[0]);
  const [frases, setFrases] = useState<string[]>(PAQUETES[0].frases);
  const [estado, accion, pendiente] = useActionState<EstadoTexto, FormData>(
    guardarTexto,
    {},
  );

  function elegir(id: string) {
    const p = PAQUETES.find((x) => x.id === id)!;
    setPaquete(p);
    setFrases(p.frases);
  }

  function cambiar(i: number, valor: string) {
    setFrases((antes) => antes.map((f, j) => (j === i ? valor : f)));
  }

  return (
    <main className="mx-auto max-w-lg px-6 py-10">
      <h1 className="text-2xl font-semibold">¿Qué quieres decirte?</h1>

      <div className="mt-6 flex flex-wrap gap-2">
        {PAQUETES.map((p) => (
          <button
            key={p.id}
            type="button"
            onClick={() => elegir(p.id)}
            className={`rounded-full border px-4 py-2 text-sm ${
              p.id === paquete.id ? "border-neutral-900 bg-neutral-900 text-white" : "border-neutral-300"
            }`}
          >
            {p.nombre}
          </button>
        ))}
      </div>

      <p className="mt-3 text-sm text-neutral-500">{paquete.descripcion}</p>

      <form action={accion} className="mt-6 flex flex-col gap-3">
        <input type="hidden" name="nombre" value={paquete.nombre} />
        {frases.map((f, i) => (
          <textarea
            key={i}
            name="frase"
            value={f}
            onChange={(e) => cambiar(i, e.target.value)}
            rows={2}
            maxLength={120}
            className="rounded-xl border border-neutral-300 px-4 py-3"
          />
        ))}
        <button
          type="button"
          onClick={() => setFrases((a) => (a.length < 12 ? [...a, ""] : a))}
          className="self-start text-sm text-neutral-500 underline"
        >
          Agregar una frase mía
        </button>

        {estado.error && <p className="text-sm text-red-600">{estado.error}</p>}
        <button
          type="submit"
          disabled={pendiente}
          className="mt-4 rounded-xl bg-neutral-900 px-4 py-3 text-white disabled:opacity-50"
        >
          {pendiente ? "Guardando…" : "Ya está, vamos a grabar"}
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Paso 7: Escribir el grabador**

Crear `src/app/(app)/grabar/[textoId]/Grabador.tsx`:

```tsx
"use client";
import { useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { supabaseNavegador } from "@/lib/supabase/cliente";
import { formatoSoportado, extensionDe } from "@/lib/audio/grabacion";

type Estado = "listo" | "grabando" | "revisando" | "subiendo";

export function Grabador({
  textoId,
  perfilId,
  frases,
}: {
  textoId: string;
  perfilId: string;
  frases: string[];
}) {
  const router = useRouter();
  const [estado, setEstado] = useState<Estado>("listo");
  const [error, setError] = useState<string | null>(null);
  const [prueba, setPrueba] = useState<string | null>(null);
  const grabadora = useRef<MediaRecorder | null>(null);
  const trozos = useRef<Blob[]>([]);
  const mime = useRef<string>("");

  async function empezar() {
    setError(null);
    const formato = formatoSoportado();
    if (!formato) {
      setError("Tu navegador no permite grabar. Prueba con Chrome o Safari actualizado.");
      return;
    }
    try {
      const pista = await navigator.mediaDevices.getUserMedia({
        audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true },
      });
      mime.current = formato;
      trozos.current = [];
      const rec = new MediaRecorder(pista, { mimeType: formato });
      rec.ondataavailable = (e) => e.data.size > 0 && trozos.current.push(e.data);
      rec.onstop = () => {
        pista.getTracks().forEach((t) => t.stop());
        const blob = new Blob(trozos.current, { type: mime.current });
        setPrueba(URL.createObjectURL(blob));
        setEstado("revisando");
      };
      grabadora.current = rec;
      rec.start();
      setEstado("grabando");
    } catch {
      setError("No pudimos usar el micrófono. Revisa los permisos del navegador.");
    }
  }

  function parar() {
    grabadora.current?.stop();
  }

  async function subir() {
    setEstado("subiendo");
    const supabase = supabaseNavegador();
    const blob = new Blob(trozos.current, { type: mime.current });
    const ruta = `${perfilId}/${textoId}/cruda.${extensionDe(mime.current)}`;

    const { error: fallo } = await supabase.storage
      .from("voces")
      .upload(ruta, blob, { contentType: mime.current, upsert: true });

    if (fallo) {
      setError("No pudimos guardar la grabación. Revisa tu conexión.");
      setEstado("revisando");
      return;
    }

    const { data, error: fallo2 } = await supabase
      .from("grabaciones")
      .insert({ perfil_id: perfilId, texto_id: textoId, estado: "cruda", ruta_cruda: ruta })
      .select("id")
      .single();

    if (fallo2 || !data) {
      setError("Se guardó el audio pero no la ficha. Inténtalo de nuevo.");
      setEstado("revisando");
      return;
    }

    router.push(`/mezclar/${data.id}`);
  }

  return (
    <div className="flex flex-col gap-6">
      <ol className="flex flex-col gap-4 text-2xl leading-relaxed">
        {frases.map((f, i) => (
          <li key={i}>{f}</li>
        ))}
      </ol>

      {error && <p className="text-sm text-red-600">{error}</p>}

      {estado === "listo" && (
        <button onClick={empezar} className="rounded-xl bg-red-600 px-4 py-4 text-white">
          Grabar
        </button>
      )}
      {estado === "grabando" && (
        <button onClick={parar} className="rounded-xl bg-neutral-900 px-4 py-4 text-white">
          Ya terminé
        </button>
      )}
      {estado === "revisando" && prueba && (
        <div className="flex flex-col gap-3">
          <audio controls src={prueba} className="w-full" />
          <button onClick={subir} className="rounded-xl bg-neutral-900 px-4 py-3 text-white">
            Me gusta, seguir
          </button>
          <button onClick={() => setEstado("listo")} className="text-sm text-neutral-500 underline">
            Volver a grabar
          </button>
        </div>
      )}
      {estado === "subiendo" && <p className="text-neutral-500">Guardando tu voz…</p>}
    </div>
  );
}
```

Crear `src/app/(app)/grabar/[textoId]/page.tsx`:

```tsx
import { redirect, notFound } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";
import { Grabador } from "./Grabador";

export default async function Grabar({
  params,
}: {
  params: Promise<{ textoId: string }>;
}) {
  const { textoId } = await params;
  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  const { data: texto } = await supabase
    .from("textos")
    .select("id, frases")
    .eq("id", textoId)
    .single();

  if (!texto) notFound();

  return (
    <main className="mx-auto max-w-lg px-6 py-10">
      <p className="text-sm text-neutral-500">
        Léelo completo, sin apuro. Si te trabas, sigue: después lo limpiamos.
      </p>
      <div className="mt-6">
        <Grabador textoId={texto.id} perfilId={user.id} frases={texto.frases} />
      </div>
    </main>
  );
}
```

- [ ] **Paso 8: Probar en el celular**

Run: `npm run dev -- --hostname 0.0.0.0`

Desde un iPhone y desde un Android en la misma red, entrar a `http://<ip-de-la-compu>:3000/estudio` y verificar: se elige paquete, se edita una frase, se graba, se escucha la prueba, y al aceptar aparece el archivo en el bucket `voces` y una fila en `voz.grabaciones` con estado `cruda`.

⚠️ Safari exige HTTPS para el micrófono salvo en `localhost`. Si el iPhone no deja grabar, usar `npx localtunnel --port 3000` o probar esa parte ya desplegada en Vercel.

- [ ] **Paso 9: Commit**

```bash
git add src/lib/audio/grabacion.ts src/lib/audio/grabacion.test.ts "src/app/(app)/estudio" "src/app/(app)/grabar"
git commit -m "Estudio para elegir el texto y grabador con teleprompter"
```

---

## Tarea 7: Pulido con IA

**Depende de la Tarea 0.** No empezar sin el servicio elegido.

**Files:**
- Create: `src/lib/pulido/proveedor.ts`, `src/lib/pulido/elegido.ts`, `src/app/api/pulir/route.ts`, `src/app/api/pulir/estado/route.ts`
- Test: `src/lib/pulido/proveedor.test.ts`

**Interfaces:**
- Consumes: `supabaseAdmin()`, `exigirEntorno`.
- Produces:
  - `type ResultadoPulido = { ok: true; audio: ArrayBuffer; tipo: string } | { ok: false; motivo: string }`
  - `interface ProveedorDePulido { nombre: string; pulir(audio: Blob, tipo: string): Promise<ResultadoPulido> }`
  - `proveedorElegido(): ProveedorDePulido`
  - `POST /api/pulir` con `{ grabacionId }` → `{ estado }`
  - `GET /api/pulir/estado?grabacionId=…` → `{ estado, error? }`

- [ ] **Paso 1: Escribir la prueba de la interfaz**

Crear `src/lib/pulido/proveedor.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { interpretarRespuesta } from "./proveedor.ts";

test("una respuesta buena entrega el audio", async () => {
  const cuerpo = new Uint8Array([1, 2, 3]);
  const resp = new Response(cuerpo, {
    status: 200,
    headers: { "content-type": "audio/mpeg" },
  });
  const r = await interpretarRespuesta(resp);
  assert.equal(r.ok, true);
  if (r.ok) {
    assert.equal(r.tipo, "audio/mpeg");
    assert.equal(r.audio.byteLength, 3);
  }
});

test("un error del servicio no revienta: devuelve el motivo", async () => {
  const resp = new Response("cuota agotada", { status: 402 });
  const r = await interpretarRespuesta(resp);
  assert.equal(r.ok, false);
  if (!r.ok) assert.match(r.motivo, /402/);
});

test("una respuesta vacía se trata como fallo", async () => {
  const resp = new Response(new Uint8Array([]), {
    status: 200,
    headers: { "content-type": "audio/mpeg" },
  });
  const r = await interpretarRespuesta(resp);
  assert.equal(r.ok, false);
});
```

- [ ] **Paso 2: Correr y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./proveedor.ts`.

- [ ] **Paso 3: Escribir la interfaz del proveedor**

Crear `src/lib/pulido/proveedor.ts`:

```ts
/**
 * Contrato del servicio que deja la voz sonando a estudio.
 *
 * Está detrás de una interfaz porque el proveedor se eligió comparando
 * calidad y precio (ver spec, punto 7) y puede cambiar: si el elegido sube
 * de precio o baja de calidad, se cambia este archivo y nada más.
 */
export type ResultadoPulido =
  | { ok: true; audio: ArrayBuffer; tipo: string }
  | { ok: false; motivo: string };

export interface ProveedorDePulido {
  nombre: string;
  pulir(audio: Blob, tipo: string): Promise<ResultadoPulido>;
}

/** Traduce la respuesta HTTP del servicio a un resultado nuestro. */
export async function interpretarRespuesta(resp: Response): Promise<ResultadoPulido> {
  if (!resp.ok) {
    const detalle = (await resp.text()).slice(0, 300);
    return { ok: false, motivo: `El servicio respondió ${resp.status}: ${detalle}` };
  }
  const audio = await resp.arrayBuffer();
  if (audio.byteLength === 0) {
    return { ok: false, motivo: "El servicio devolvió un audio vacío" };
  }
  return { ok: true, audio, tipo: resp.headers.get("content-type") ?? "audio/mpeg" };
}
```

- [ ] **Paso 4: Correr y ver que pasa**

Run: `npm test`
Expected: PASS, 39 pruebas en total.

- [ ] **Paso 5: Escribir el proveedor elegido**

Crear `src/lib/pulido/elegido.ts`. El ejemplo usa el Voice Isolator de ElevenLabs; **si la Tarea 0 eligió otro, cambiar la URL, la cabecera de autenticación y el nombre del campo, dejando igual la firma de `pulir`**:

```ts
import "server-only";
import { exigirEntorno } from "@/lib/entorno";
import { interpretarRespuesta, type ProveedorDePulido } from "./proveedor";

const elevenlabs: ProveedorDePulido = {
  nombre: "elevenlabs",
  async pulir(audio, tipo) {
    const cuerpo = new FormData();
    cuerpo.append("audio", audio, `voz.${tipo.includes("mp4") ? "m4a" : "webm"}`);

    const resp = await fetch("https://api.elevenlabs.io/v1/audio-isolation", {
      method: "POST",
      headers: { "xi-api-key": exigirEntorno("PULIDO_API_KEY") },
      body: cuerpo,
    });

    return interpretarRespuesta(resp);
  },
};

export function proveedorElegido(): ProveedorDePulido {
  return elevenlabs;
}
```

- [ ] **Paso 6: Escribir el disparador del pulido**

Crear `src/app/api/pulir/route.ts`:

```ts
import { NextResponse } from "next/server";
import { supabaseServidor } from "@/lib/supabase/servidor";
import { supabaseAdmin } from "@/lib/supabase/admin";
import { proveedorElegido } from "@/lib/pulido/elegido";

/**
 * El pulido puede tardar más de un minuto para una lectura larga. Con el
 * plan Pro de Vercel se puede pedir hasta 300 s; con el valor por defecto
 * (60 s) la petición se cortaría a la mitad y la grabación quedaría colgada
 * en "puliendo" para siempre.
 */
export const maxDuration = 300;

export async function POST(peticion: Request) {
  const { grabacionId } = (await peticion.json()) as { grabacionId?: string };
  if (!grabacionId) {
    return NextResponse.json({ error: "Falta grabacionId" }, { status: 400 });
  }

  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "Sin sesión" }, { status: 401 });

  // Se lee con el cliente del usuario: RLS garantiza que la grabación es suya.
  const { data: grabacion } = await supabase
    .from("grabaciones")
    .select("id, estado, ruta_cruda")
    .eq("id", grabacionId)
    .single();

  if (!grabacion?.ruta_cruda) {
    return NextResponse.json({ error: "Grabación no encontrada" }, { status: 404 });
  }
  if (grabacion.estado === "lista") {
    return NextResponse.json({ estado: "lista" });
  }

  const admin = supabaseAdmin();
  await admin.from("grabaciones").update({ estado: "puliendo", error: null, actualizado_en: new Date().toISOString() }).eq("id", grabacionId);

  const { data: archivo, error: falloDescarga } = await admin.storage
    .from("voces")
    .download(grabacion.ruta_cruda);

  if (falloDescarga || !archivo) {
    await admin.from("grabaciones").update({ estado: "fallida", error: "No se pudo leer la grabación" }).eq("id", grabacionId);
    return NextResponse.json({ estado: "fallida" }, { status: 500 });
  }

  const resultado = await proveedorElegido().pulir(archivo, archivo.type);

  if (!resultado.ok) {
    await admin.from("grabaciones").update({ estado: "fallida", error: resultado.motivo.slice(0, 500) }).eq("id", grabacionId);
    return NextResponse.json({ estado: "fallida" }, { status: 502 });
  }

  const rutaMaster = grabacion.ruta_cruda.replace(/cruda\.\w+$/, "master.mp3");
  const { error: falloSubida } = await admin.storage
    .from("voces")
    .upload(rutaMaster, resultado.audio, { contentType: resultado.tipo, upsert: true });

  if (falloSubida) {
    await admin.from("grabaciones").update({ estado: "fallida", error: "No se pudo guardar la voz limpia" }).eq("id", grabacionId);
    return NextResponse.json({ estado: "fallida" }, { status: 500 });
  }

  // La cruda ya no hace falta y es la más comprometedora: se borra (spec, punto 5).
  await admin.storage.from("voces").remove([grabacion.ruta_cruda]);

  await admin
    .from("grabaciones")
    .update({ estado: "lista", ruta_master: rutaMaster, ruta_cruda: null, actualizado_en: new Date().toISOString() })
    .eq("id", grabacionId);

  return NextResponse.json({ estado: "lista" });
}
```

- [ ] **Paso 7: Escribir la consulta de estado**

Crear `src/app/api/pulir/estado/route.ts`:

```ts
import { NextResponse } from "next/server";
import { supabaseServidor } from "@/lib/supabase/servidor";

export async function GET(peticion: Request) {
  const id = new URL(peticion.url).searchParams.get("grabacionId");
  if (!id) return NextResponse.json({ error: "Falta grabacionId" }, { status: 400 });

  const supabase = await supabaseServidor();
  const { data } = await supabase
    .from("grabaciones")
    .select("estado, error")
    .eq("id", id)
    .single();

  if (!data) return NextResponse.json({ error: "No encontrada" }, { status: 404 });
  return NextResponse.json(data);
}
```

- [ ] **Paso 8: Probar con la grabación real de la Tarea 6**

Con `PULIDO_API_KEY` en `.env.local` y una grabación en estado `cruda`:

```bash
curl -X POST http://localhost:3000/api/pulir \
  -H "content-type: application/json" \
  -H "cookie: <copiar las cookies de sesión del navegador>" \
  -d '{"grabacionId":"<id real>"}'
```

Expected: `{"estado":"lista"}`. Verificar en Supabase que existe `master.mp3`, que la cruda desapareció, y **escuchar el master**: tiene que sonar mejor que el original.

- [ ] **Paso 9: Commit**

```bash
git add src/lib/pulido src/app/api/pulir
git commit -m "Pulido de la voz con el servicio elegido y borrado de la toma cruda"
```

---

## Tarea 8: El reproductor y la pantalla de mezcla

Aquí no hay pruebas automáticas: el navegador no se puede simular con `node:test`. La lógica que sí se podía probar ya vive en `plan.ts` y `frases.ts`; esta tarea solo la ejecuta. La verificación es manual y está detallada.

**Files:**
- Create: `src/lib/audio/reproductor.ts`, `src/app/(app)/mezclar/[grabacionId]/page.tsx`, `src/app/(app)/mezclar/[grabacionId]/Mezclador.tsx`
- Create: `public/fondos/lluvia.mp3`, `rio.mp3`, `mar.mp3`, `bosque.mp3`, `tormenta.mp3`

**Interfaces:**
- Consumes: `construirPlan`, `PlanDeMezcla`, `calcularEnergia`, `detectarFrases`.
- Produces: `class Reproductor` con `cargar(vozUrl, fondoUrl)`, `reproducir(plan, enBucle)`, `detener()`, `analizarFrases(): Frase[]`, `get duracionVoz(): number`.

- [ ] **Paso 1: Conseguir los cinco fondos**

Descargar cinco pistas de ~3 minutos, en dominio público o CC0, de Pixabay Sounds o Freesound (filtrando por licencia CC0): lluvia, río, mar, bosque y tormenta suave. Convertir a MP3 mono 128 kbps y dejarlas en `public/fondos/` con esos nombres exactos.

Crear `public/fondos/LICENCIAS.md` anotando, por cada archivo, de dónde salió y bajo qué licencia. Sin esto no se puede vender el producto.

- [ ] **Paso 2: Escribir el reproductor**

Crear `src/lib/audio/reproductor.ts`:

```ts
"use client";
import { construirPlan, type PlanDeMezcla } from "./plan";
import { calcularEnergia, detectarFrases } from "./frases";
import type { Frase } from "./plan";

/**
 * Ejecuta un plan de mezcla con Web Audio.
 *
 * No genera archivos: programa la voz y el fondo en el reloj del navegador
 * y los suena. Por eso cambiar de fondo o mover el volumen es instantáneo,
 * y por eso no existe ningún audio que se pueda extraer de la app.
 */
export class Reproductor {
  private contexto: AudioContext | null = null;
  private voz: AudioBuffer | null = null;
  private fondo: AudioBuffer | null = null;
  private fuentes: AudioBufferSourceNode[] = [];
  private temporizador: ReturnType<typeof setTimeout> | null = null;

  async cargar(vozUrl: string, fondoUrl: string): Promise<void> {
    this.contexto ??= new AudioContext();
    const [voz, fondo] = await Promise.all([
      this.bajar(vozUrl),
      this.bajar(fondoUrl),
    ]);
    this.voz = voz;
    this.fondo = fondo;
  }

  private async bajar(url: string): Promise<AudioBuffer> {
    const datos = await (await fetch(url)).arrayBuffer();
    return this.contexto!.decodeAudioData(datos);
  }

  get duracionVoz(): number {
    return this.voz?.duration ?? 0;
  }

  /** Corta la voz master en frases mirando su energía. */
  analizarFrases(): Frase[] {
    if (!this.voz) return [];
    const canal = this.voz.getChannelData(0);
    const muestrasPorVentana = Math.round(this.voz.sampleRate * 0.02);
    const energia = calcularEnergia(canal, muestrasPorVentana);
    return detectarFrases(energia, 0.02);
  }

  reproducir(plan: PlanDeMezcla, enBucle: boolean): void {
    if (!this.contexto || !this.voz || !this.fondo) return;
    this.detener();
    void this.contexto.resume(); // iOS arranca el contexto suspendido

    const ahora = this.contexto.currentTime + 0.1;

    const gananciaFondo = this.contexto.createGain();
    gananciaFondo.connect(this.contexto.destination);
    gananciaFondo.gain.setValueAtTime(0, ahora);
    gananciaFondo.gain.linearRampToValueAtTime(plan.fondo.ganancia, ahora + plan.fondo.entrada);
    gananciaFondo.gain.setValueAtTime(plan.fondo.ganancia, ahora + plan.duracionTotal - plan.fondo.salida);
    gananciaFondo.gain.linearRampToValueAtTime(0, ahora + plan.duracionTotal);

    const fuenteFondo = this.contexto.createBufferSource();
    fuenteFondo.buffer = this.fondo;
    fuenteFondo.loop = true; // el fondo puede ser más corto que la pieza
    fuenteFondo.connect(gananciaFondo);
    fuenteFondo.start(ahora, 0, plan.duracionTotal);
    this.fuentes.push(fuenteFondo);

    for (const bloque of plan.voz) {
      const fuente = this.contexto.createBufferSource();
      fuente.buffer = this.voz;
      fuente.connect(this.contexto.destination);
      fuente.start(ahora + bloque.entraEn, bloque.desde, bloque.hasta - bloque.desde);
      this.fuentes.push(fuente);
    }

    if (enBucle) {
      this.temporizador = setTimeout(
        () => this.reproducir(plan, true),
        plan.duracionTotal * 1000,
      );
    }
  }

  detener(): void {
    if (this.temporizador) clearTimeout(this.temporizador);
    this.temporizador = null;
    this.fuentes.forEach((f) => {
      try {
        f.stop();
      } catch {
        // Ya había terminado sola; no es un error.
      }
    });
    this.fuentes = [];
  }
}

export { construirPlan };
```

- [ ] **Paso 3: Escribir la pantalla de mezcla**

Crear `src/app/(app)/mezclar/[grabacionId]/Mezclador.tsx`:

```tsx
"use client";
import { useEffect, useRef, useState } from "react";
import { Reproductor } from "@/lib/audio/reproductor";
import { construirPlan, type Frase } from "@/lib/audio/plan";

const FONDOS = [
  { id: "lluvia", nombre: "Lluvia" },
  { id: "rio", nombre: "Río" },
  { id: "mar", nombre: "Mar" },
  { id: "bosque", nombre: "Bosque" },
  { id: "tormenta", nombre: "Tormenta suave" },
];

export function Mezclador({
  vozUrl,
  cortesGuardados,
}: {
  vozUrl: string;
  cortesGuardados: Frase[] | null;
}) {
  const reproductor = useRef<Reproductor | null>(null);
  const [fondo, setFondo] = useState("lluvia");
  const [ganancia, setGanancia] = useState(0.35);
  const [pausa, setPausa] = useState(2);
  const [frases, setFrases] = useState<Frase[]>(cortesGuardados ?? []);
  const [sonando, setSonando] = useState(false);
  const [cargando, setCargando] = useState(true);

  useEffect(() => {
    const r = new Reproductor();
    reproductor.current = r;
    r.cargar(vozUrl, `/fondos/${fondo}.mp3`).then(() => {
      if (frases.length === 0) setFrases(r.analizarFrases());
      setCargando(false);
    });
    return () => r.detener();
    // Solo al montar: el cambio de fondo se maneja aparte.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  async function cambiarFondo(id: string) {
    setFondo(id);
    setCargando(true);
    await reproductor.current!.cargar(vozUrl, `/fondos/${id}.mp3`);
    setCargando(false);
    if (sonando) sonar(id);
  }

  function sonar(fondoId = fondo) {
    const plan = construirPlan(frases, {
      fondo: fondoId,
      gananciaFondo: ganancia,
      pausaSeg: pausa,
      orden: "original",
    });
    reproductor.current!.reproducir(plan, true);
    setSonando(true);
  }

  function parar() {
    reproductor.current!.detener();
    setSonando(false);
  }

  const plan = frases.length
    ? construirPlan(frases, { fondo, gananciaFondo: ganancia, pausaSeg: pausa, orden: "original" })
    : null;

  return (
    <div className="flex flex-col gap-8">
      <div>
        <p className="text-sm text-neutral-500">Fondo</p>
        <div className="mt-2 flex flex-wrap gap-2">
          {FONDOS.map((f) => (
            <button
              key={f.id}
              onClick={() => cambiarFondo(f.id)}
              className={`rounded-full border px-4 py-2 text-sm ${
                f.id === fondo ? "border-neutral-900 bg-neutral-900 text-white" : "border-neutral-300"
              }`}
            >
              {f.nombre}
            </button>
          ))}
        </div>
      </div>

      <label className="flex flex-col gap-2">
        <span className="text-sm text-neutral-500">
          Qué tan presente está el fondo: {Math.round(ganancia * 100)}%
        </span>
        <input
          type="range"
          min={0}
          max={0.8}
          step={0.05}
          value={ganancia}
          onChange={(e) => setGanancia(Number(e.target.value))}
        />
      </label>

      <label className="flex flex-col gap-2">
        <span className="text-sm text-neutral-500">
          Silencio entre frases: {pausa.toFixed(1)} s
        </span>
        <input
          type="range"
          min={0}
          max={8}
          step={0.5}
          value={pausa}
          onChange={(e) => setPausa(Number(e.target.value))}
        />
      </label>

      {plan && (
        <p className="text-sm text-neutral-500">
          {frases.length} frases · {Math.round(plan.duracionTotal)} segundos
        </p>
      )}

      <button
        onClick={() => (sonando ? parar() : sonar())}
        disabled={cargando || frases.length === 0}
        className="rounded-xl bg-neutral-900 px-4 py-4 text-white disabled:opacity-50"
      >
        {cargando ? "Preparando…" : sonando ? "Parar" : "Escuchar"}
      </button>
    </div>
  );
}
```

Crear `src/app/(app)/mezclar/[grabacionId]/page.tsx`:

```tsx
import { redirect, notFound } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";
import { Mezclador } from "./Mezclador";

export default async function Mezclar({
  params,
}: {
  params: Promise<{ grabacionId: string }>;
}) {
  const { grabacionId } = await params;
  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  const { data: grabacion } = await supabase
    .from("grabaciones")
    .select("id, estado, ruta_master, cortes")
    .eq("id", grabacionId)
    .single();

  if (!grabacion) notFound();
  if (grabacion.estado !== "lista" || !grabacion.ruta_master) {
    redirect(`/grabar/esperando?grabacion=${grabacionId}`);
  }

  // URL firmada de una hora: la voz nunca se sirve por una dirección
  // pública ni adivinable.
  const { data: firmada } = await supabase.storage
    .from("voces")
    .createSignedUrl(grabacion.ruta_master, 3600);

  if (!firmada) notFound();

  return (
    <main className="mx-auto max-w-lg px-6 py-10">
      <h1 className="text-2xl font-semibold">Tu audio</h1>
      <div className="mt-8">
        <Mezclador vozUrl={firmada.signedUrl} cortesGuardados={grabacion.cortes} />
      </div>
    </main>
  );
}
```

- [ ] **Paso 4: Verificación manual (esta es la prueba de fuego del producto)**

Run: `npm run dev`

Con una grabación en estado `lista`, abrir `/mezclar/<id>` y comprobar una por una:

1. El fondo entra **antes** que la voz (2 segundos de lluvia sola).
2. Las frases se oyen **completas**, sin cortar la primera ni la última sílaba.
3. Entre frase y frase hay silencio, y al mover el control la separación cambia de verdad.
4. Bajar el control del fondo lo aleja; subirlo lo acerca sin tapar la voz.
5. Cambiar de lluvia a río tarda menos de 2 segundos.
6. Al terminar, la pieza vuelve a empezar sola, sin silencio raro ni corte brusco.
7. En iPhone, el primer toque en "Escuchar" arranca el sonido (si no, el contexto quedó suspendido).

Si el corte de frases queda mal —frases pegadas o partidas a la mitad—, ajustar `silencioMinSeg` y `umbralRelativo` en `analizarFrases()`, **agregando primero una prueba en `frases.test.ts`** que reproduzca el caso.

- [ ] **Paso 5: Commit**

```bash
git add src/lib/audio/reproductor.ts "src/app/(app)/mezclar" public/fondos
git commit -m "Reproductor Web Audio y pantalla de mezcla en vivo"
```

---

## Tarea 9: Pantalla de espera del pulido y guardar los cortes

**Files:**
- Create: `src/app/(app)/grabar/esperando/page.tsx`, `src/app/(app)/grabar/esperando/Espera.tsx`
- Modify: `src/app/(app)/mezclar/[grabacionId]/Mezclador.tsx` (guardar los cortes la primera vez)
- Create: `src/app/(app)/mezclar/[grabacionId]/acciones.ts`

**Interfaces:**
- Consumes: `POST /api/pulir`, `GET /api/pulir/estado`.
- Produces: server action `guardarCortes(grabacionId: string, cortes: Frase[]): Promise<void>`.

- [ ] **Paso 1: Escribir la pantalla de espera**

Crear `src/app/(app)/grabar/esperando/Espera.tsx`:

```tsx
"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";

const FRASES_DE_ESPERA = [
  "Escuchando tu voz…",
  "Quitando el ruido del cuarto…",
  "Emparejando el volumen…",
  "Casi lista…",
];

export function Espera({ grabacionId }: { grabacionId: string }) {
  const router = useRouter();
  const [mensaje, setMensaje] = useState(FRASES_DE_ESPERA[0]);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    let vivo = true;

    // El pulido puede tardar; sin este mensaje que cambia, la pantalla
    // parece colgada y la gente recarga a mitad del proceso.
    const rotador = setInterval(() => {
      setMensaje((actual) => {
        const i = FRASES_DE_ESPERA.indexOf(actual);
        return FRASES_DE_ESPERA[(i + 1) % FRASES_DE_ESPERA.length];
      });
    }, 4000);

    async function arrancar() {
      await fetch("/api/pulir", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ grabacionId }),
      });
    }

    async function vigilar() {
      while (vivo) {
        const resp = await fetch(`/api/pulir/estado?grabacionId=${grabacionId}`);
        const datos = (await resp.json()) as { estado: string; error?: string };

        if (datos.estado === "lista") {
          router.push(`/mezclar/${grabacionId}`);
          return;
        }
        if (datos.estado === "fallida") {
          setError("No pudimos limpiar tu grabación. Puedes volver a grabarla.");
          return;
        }
        await new Promise((r) => setTimeout(r, 3000));
      }
    }

    arrancar().then(vigilar);

    return () => {
      vivo = false;
      clearInterval(rotador);
    };
  }, [grabacionId, router]);

  if (error) {
    return (
      <div className="flex flex-col gap-4">
        <p className="text-red-600">{error}</p>
        <a href="/estudio" className="underline">Volver a empezar</a>
      </div>
    );
  }

  return (
    <div className="flex flex-col items-center gap-4">
      <div className="h-2 w-full overflow-hidden rounded-full bg-neutral-200">
        <div className="h-full w-1/3 animate-pulse rounded-full bg-neutral-900" />
      </div>
      <p className="text-neutral-500">{mensaje}</p>
    </div>
  );
}
```

Crear `src/app/(app)/grabar/esperando/page.tsx`:

```tsx
import { redirect } from "next/navigation";
import { Espera } from "./Espera";

export default async function Esperando({
  searchParams,
}: {
  searchParams: Promise<{ grabacion?: string }>;
}) {
  const { grabacion } = await searchParams;
  if (!grabacion) redirect("/estudio");

  return (
    <main className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center gap-6 px-6">
      <h1 className="text-2xl font-semibold">Puliendo tu voz</h1>
      <Espera grabacionId={grabacion} />
    </main>
  );
}
```

- [ ] **Paso 2: Mandar a esperar después de grabar**

En `src/app/(app)/grabar/[textoId]/Grabador.tsx`, cambiar la última línea de `subir()`:

```ts
    router.push(`/mezclar/${data.id}`);
```

por:

```ts
    router.push(`/grabar/esperando?grabacion=${data.id}`);
```

- [ ] **Paso 3: Escribir la acción que guarda los cortes**

Crear `src/app/(app)/mezclar/[grabacionId]/acciones.ts`:

```ts
"use server";
import { supabaseServidor } from "@/lib/supabase/servidor";
import type { Frase } from "@/lib/audio/plan";

/**
 * Guarda las marcas de tiempo de las frases la primera vez que se analizan.
 * Así, al volver mañana, la app no tiene que recalcularlas: abre y suena.
 */
export async function guardarCortes(grabacionId: string, cortes: Frase[]): Promise<void> {
  const supabase = await supabaseServidor();
  await supabase.from("grabaciones").update({ cortes }).eq("id", grabacionId);
}
```

- [ ] **Paso 4: Llamarla desde el mezclador**

En `src/app/(app)/mezclar/[grabacionId]/Mezclador.tsx`:

Agregar `grabacionId` a las props del componente (`{ vozUrl, cortesGuardados, grabacionId }: { vozUrl: string; cortesGuardados: Frase[] | null; grabacionId: string }`), importar la acción con `import { guardarCortes } from "./acciones";`, y dentro del `useEffect` reemplazar:

```ts
      if (frases.length === 0) setFrases(r.analizarFrases());
```

por:

```ts
      if (frases.length === 0) {
        const detectadas = r.analizarFrases();
        setFrases(detectadas);
        void guardarCortes(grabacionId, detectadas);
      }
```

En `page.tsx`, pasar la prop nueva: `<Mezclador vozUrl={firmada.signedUrl} cortesGuardados={grabacion.cortes} grabacionId={grabacion.id} />`.

- [ ] **Paso 5: Probar el recorrido completo**

Run: `npm run dev`

Desde cero: entrar → elegir paquete → grabar → ver la pantalla de espera → llegar solo a la mezcla → escuchar. Después **recargar** `/mezclar/<id>` y comprobar que suena de inmediato, sin recalcular las frases (la columna `cortes` ya tiene datos).

- [ ] **Paso 6: Commit**

```bash
git add "src/app/(app)/grabar" "src/app/(app)/mezclar"
git commit -m "Pantalla de espera del pulido y cortes de frases guardados"
```

---

## Tarea 10: Límites del plan y pago manual

**Files:**
- Create: `src/lib/planes.ts`, `src/app/(app)/cuenta/page.tsx`, `src/app/(app)/cuenta/acciones.ts`
- Test: `src/lib/planes.test.ts`
- Modify: `src/app/(app)/estudio/acciones.ts` (cortar antes de crear un texto nuevo)

**Interfaces:**
- Consumes: `supabaseServidor()`.
- Produces: `type Plan = "gratis" | "pago"`, `LIMITES`, `puedeGrabar(plan, grabacionesActuales): boolean`, `fondosPermitidos(plan): string[]`, `puedeUsarSinInternet(plan): boolean`.

- [ ] **Paso 1: Escribir las pruebas de los límites**

Crear `src/lib/planes.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { puedeGrabar, fondosPermitidos, puedeUsarSinInternet, LIMITES } from "./planes.ts";

test("el plan gratis permite una sola grabación", () => {
  assert.equal(puedeGrabar("gratis", 0), true);
  assert.equal(puedeGrabar("gratis", 1), false);
  assert.equal(puedeGrabar("gratis", 5), false);
});

test("el plan pago permite varias, hasta el tope", () => {
  assert.equal(puedeGrabar("pago", 0), true);
  assert.equal(puedeGrabar("pago", LIMITES.pago.grabaciones - 1), true);
  assert.equal(puedeGrabar("pago", LIMITES.pago.grabaciones), false);
});

test("el plan gratis solo tiene dos fondos", () => {
  assert.deepEqual(fondosPermitidos("gratis"), ["lluvia", "rio"]);
  assert.equal(fondosPermitidos("pago").length, 5);
});

test("el modo sin internet es solo del plan pago", () => {
  assert.equal(puedeUsarSinInternet("gratis"), false);
  assert.equal(puedeUsarSinInternet("pago"), true);
});
```

- [ ] **Paso 2: Correr y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./planes.ts`.

- [ ] **Paso 3: Escribir los límites**

Crear `src/lib/planes.ts`:

```ts
/**
 * Límites de cada plan.
 *
 * El único costo variable del producto es el pulido con IA, y se paga una
 * vez por grabación. Por eso el límite del plan gratis son las GRABACIONES
 * y no el uso: armar audios, cambiar fondos y escuchar mil veces no le
 * cuesta nada a nadie.
 */
export type Plan = "gratis" | "pago";

export const LIMITES = {
  gratis: { grabaciones: 1, fondos: ["lluvia", "rio"], sinInternet: false },
  pago: {
    grabaciones: 20,
    fondos: ["lluvia", "rio", "mar", "bosque", "tormenta"],
    sinInternet: true,
  },
} as const;

export function puedeGrabar(plan: Plan, grabacionesActuales: number): boolean {
  return grabacionesActuales < LIMITES[plan].grabaciones;
}

export function fondosPermitidos(plan: Plan): string[] {
  return [...LIMITES[plan].fondos];
}

export function puedeUsarSinInternet(plan: Plan): boolean {
  return LIMITES[plan].sinInternet;
}
```

- [ ] **Paso 4: Correr y ver que pasa**

Run: `npm test`
Expected: PASS, 43 pruebas en total.

- [ ] **Paso 5: Aplicar el límite antes de crear un texto**

En `src/app/(app)/estudio/acciones.ts`, agregar el import:

```ts
import { puedeGrabar, type Plan } from "@/lib/planes";
```

y justo después de obtener el usuario (antes del `insert` en `textos`):

```ts
  const { data: perfil } = await supabase
    .from("perfiles")
    .select("plan")
    .eq("id", user.id)
    .single();

  const { count } = await supabase
    .from("grabaciones")
    .select("id", { count: "exact", head: true })
    .eq("perfil_id", user.id);

  if (!puedeGrabar((perfil?.plan ?? "gratis") as Plan, count ?? 0)) {
    return {
      error: "Ya usaste tu grabación gratis. Desbloquea más grabaciones desde tu cuenta.",
    };
  }
```

- [ ] **Paso 6: Escribir la acción de solicitar el pago**

Crear `src/app/(app)/cuenta/acciones.ts`:

```ts
"use server";
import { redirect } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";
import { leerEntorno } from "@/lib/entorno";

/**
 * Pago manual mientras se valida el producto: se manda a la persona a un
 * link de dLocal y la cuenta se activa a mano desde Supabase. Construir
 * facturación automática antes de saber si la gente paga sería trabajo
 * tirado.
 */
export async function pedirDesbloqueo(): Promise<void> {
  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  const link = leerEntorno("LINK_PAGO") ?? "https://wa.me/593983745757";
  redirect(link);
}

/** Borra todo: voces, textos y perfil. Sin retenciones de cortesía. */
export async function borrarTodo(): Promise<void> {
  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  const { data: archivos } = await supabase.storage.from("voces").list(user.id, { limit: 100 });
  const carpetas = archivos ?? [];

  for (const carpeta of carpetas) {
    const { data: dentro } = await supabase.storage
      .from("voces")
      .list(`${user.id}/${carpeta.name}`, { limit: 100 });
    const rutas = (dentro ?? []).map((a) => `${user.id}/${carpeta.name}/${a.name}`);
    if (rutas.length) await supabase.storage.from("voces").remove(rutas);
  }

  // Las tablas caen solas: todo cuelga de perfiles con on delete cascade.
  await supabase.from("perfiles").delete().eq("id", user.id);
  await supabase.auth.signOut();
  redirect("/");
}
```

- [ ] **Paso 7: Escribir la pantalla de cuenta**

Crear `src/app/(app)/cuenta/page.tsx`:

```tsx
import { redirect } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";
import { ocultarCelular } from "@/lib/telefono";
import { LIMITES, type Plan } from "@/lib/planes";
import { pedirDesbloqueo, borrarTodo } from "./acciones";

export default async function Cuenta() {
  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  const { data: perfil } = await supabase
    .from("perfiles")
    .select("celular, plan")
    .eq("id", user.id)
    .single();

  const { count } = await supabase
    .from("grabaciones")
    .select("id", { count: "exact", head: true })
    .eq("perfil_id", user.id);

  const plan = (perfil?.plan ?? "gratis") as Plan;

  return (
    <main className="mx-auto flex max-w-lg flex-col gap-8 px-6 py-10">
      <div>
        <h1 className="text-2xl font-semibold">Tu cuenta</h1>
        <p className="mt-2 text-neutral-500">{ocultarCelular(perfil?.celular ?? "")}</p>
      </div>

      <div className="rounded-xl border border-neutral-200 p-4">
        <p className="font-medium">Plan {plan}</p>
        <p className="mt-1 text-sm text-neutral-500">
          {count ?? 0} de {LIMITES[plan].grabaciones} grabaciones usadas
        </p>
        {plan === "gratis" && (
          <form action={pedirDesbloqueo} className="mt-4">
            <button className="rounded-xl bg-neutral-900 px-4 py-3 text-white">
              Quiero más grabaciones
            </button>
          </form>
        )}
      </div>

      <form action={borrarTodo}>
        <button className="text-sm text-red-600 underline">
          Borrar mi voz y mi cuenta para siempre
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Paso 8: Probar los límites**

Run: `npm run dev`

1. Con una cuenta que ya tiene una grabación, intentar crear otro texto: debe aparecer "Ya usaste tu grabación gratis…".
2. Cambiar a mano `voz.perfiles.plan` a `'pago'` en Supabase y comprobar que ahora sí deja.
3. Tocar "Borrar mi voz y mi cuenta": verificar en Supabase que desaparecieron la fila de `perfiles`, las de `grabaciones` y los archivos del bucket.

- [ ] **Paso 9: Commit**

```bash
git add src/lib/planes.ts src/lib/planes.test.ts "src/app/(app)/cuenta" "src/app/(app)/estudio/acciones.ts"
git commit -m "Limites de plan gratis y pago, pago manual y borrado real de la cuenta"
```

---

## Tarea 11: Consentimiento y aviso de privacidad

**Files:**
- Create: `src/app/(app)/consentimiento/page.tsx`, `src/app/(app)/consentimiento/acciones.ts`, `src/app/privacidad/page.tsx`
- Modify: `src/app/(app)/grabar/[textoId]/page.tsx` (exigir consentimiento antes de grabar)

**Interfaces:**
- Consumes: `supabaseServidor()`.
- Produces: server action `aceptarConsentimiento(): Promise<void>` que sella `voz.perfiles.consentimiento_en`.

- [ ] **Paso 1: Escribir la acción**

Crear `src/app/(app)/consentimiento/acciones.ts`:

```ts
"use server";
import { redirect } from "next/navigation";
import { supabaseServidor } from "@/lib/supabase/servidor";

export async function aceptarConsentimiento(datos: FormData): Promise<void> {
  const volverA = String(datos.get("volverA") ?? "/estudio");
  const supabase = await supabaseServidor();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/ingresar");

  await supabase
    .from("perfiles")
    .update({ consentimiento_en: new Date().toISOString() })
    .eq("id", user.id);

  redirect(volverA);
}
```

- [ ] **Paso 2: Escribir la pantalla de consentimiento**

Crear `src/app/(app)/consentimiento/page.tsx`:

```tsx
import { aceptarConsentimiento } from "./acciones";

export default async function Consentimiento({
  searchParams,
}: {
  searchParams: Promise<{ volverA?: string }>;
}) {
  const { volverA } = await searchParams;

  return (
    <main className="mx-auto flex max-w-lg flex-col gap-6 px-6 py-10">
      <h1 className="text-2xl font-semibold">Antes de grabar tu voz</h1>

      <div className="flex flex-col gap-4 text-neutral-700">
        <p>
          Tu voz es tuya. Esto es lo que hacemos con ella, sin letra chica:
        </p>
        <ul className="flex flex-col gap-3 pl-5 [&>li]:list-disc">
          <li>
            <strong>La guardamos para ti.</strong> Queda en un almacén privado al
            que nadie llega por una dirección de internet. Ninguna persona de
            nuestro equipo la escucha.
          </li>
          <li>
            <strong>La limpiamos una vez.</strong> Pasa por un servicio externo que
            le quita el ruido. Ese servicio no entrena programas con tu voz y la
            borra al terminar.
          </li>
          <li>
            <strong>Borramos la toma original.</strong> Apenas queda lista la versión
            limpia, la grabación cruda se elimina.
          </li>
          <li>
            <strong>Nunca la compartimos ni la vendemos.</strong> A nadie, por
            ningún motivo.
          </li>
          <li>
            <strong>Puedes borrarla cuando quieras</strong>, desde tu cuenta, en un
            toque. Borrar es borrar.
          </li>
        </ul>
        <p className="text-sm text-neutral-500">
          Puedes leer el <a href="/privacidad" className="underline">aviso completo</a>.
        </p>
      </div>

      <form action={aceptarConsentimiento}>
        <input type="hidden" name="volverA" value={volverA ?? "/estudio"} />
        <button className="w-full rounded-xl bg-neutral-900 px-4 py-4 text-white">
          Entiendo, quiero grabar
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Paso 3: Exigirlo antes de grabar**

En `src/app/(app)/grabar/[textoId]/page.tsx`, después de obtener el usuario y antes de leer el texto:

```tsx
  const { data: perfil } = await supabase
    .from("perfiles")
    .select("consentimiento_en")
    .eq("id", user.id)
    .single();

  if (!perfil?.consentimiento_en) {
    redirect(`/consentimiento?volverA=/grabar/${textoId}`);
  }
```

- [ ] **Paso 4: Escribir el aviso de privacidad**

Crear `src/app/privacidad/page.tsx`:

```tsx
export default function Privacidad() {
  return (
    <main className="mx-auto flex max-w-2xl flex-col gap-6 px-6 py-12 text-neutral-700">
      <h1 className="text-3xl font-semibold text-neutral-900">Aviso de privacidad</h1>
      <p className="text-sm text-neutral-500">Actualizado el 26 de julio de 2026</p>

      <h2 className="text-xl font-medium text-neutral-900">Qué guardamos</h2>
      <p>
        Tu número de celular, el texto de las afirmaciones que escribes o editas, y
        la grabación de tu voz ya limpia. Nada más. No pedimos nombre, correo,
        cédula ni dirección.
      </p>

      <h2 className="text-xl font-medium text-neutral-900">Para qué</h2>
      <p>
        Únicamente para que puedas escuchar tus audios dentro de la app. Tu voz no
        se usa para entrenar programas, no se comparte con terceros fuera del
        servicio que la limpia, y no se vende a nadie.
      </p>

      <h2 className="text-xl font-medium text-neutral-900">Quién más la toca</h2>
      <p>
        Un solo servicio externo, el que limpia el audio, y solo durante ese
        proceso. Lo elegimos exigiendo que no entrene modelos con lo que recibe y
        que borre el archivo al terminar. La grabación original se elimina de
        nuestros servidores apenas queda lista la versión limpia.
      </p>

      <h2 className="text-xl font-medium text-neutral-900">Cuánto tiempo</h2>
      <p>
        Hasta que tú decidas. Desde tu cuenta puedes borrar tu voz y tu cuenta
        entera en un toque, y se elimina de inmediato: no guardamos copias de
        respaldo ni conservamos nada &quot;por treinta días&quot;.
      </p>

      <h2 className="text-xl font-medium text-neutral-900">Un límite que debes conocer</h2>
      <p>
        Tus audios se escuchan dentro de la app y no se pueden descargar ni
        compartir desde ella. Aun así, cualquier sonido que salga de un navegador
        puede ser capturado por alguien con conocimientos técnicos. Lo hacemos
        difícil, pero preferimos decírtelo a prometerte algo que no podemos
        garantizar.
      </p>

      <h2 className="text-xl font-medium text-neutral-900">Contacto</h2>
      <p>Escríbenos por WhatsApp al +593 98 374 5757.</p>
    </main>
  );
}
```

- [ ] **Paso 5: Probar el circuito**

Run: `npm run dev`

Con una cuenta nueva, ir directo a grabar: debe aparecer primero el consentimiento. Al aceptar, vuelve exactamente a la pantalla de grabación. Verificar que `voz.perfiles.consentimiento_en` quedó sellado y que la segunda vez ya no lo pide.

- [ ] **Paso 6: Commit**

```bash
git add "src/app/(app)/consentimiento" src/app/privacidad "src/app/(app)/grabar"
git commit -m "Consentimiento antes de grabar y aviso de privacidad"
```

---

## Tarea 12: Escuchar sin internet

**Files:**
- Create: `src/lib/sinInternet.ts`, `public/sw.js`, `src/app/manifest.ts`
- Test: `src/lib/sinInternet.test.ts`
- Modify: `src/app/(app)/mezclar/[grabacionId]/Mezclador.tsx` (botón de guardar en el celular)

**Interfaces:**
- Consumes: `puedeUsarSinInternet` de `@/lib/planes`.
- Produces: `nombreDeCache(grabacionId: string): string`, `guardarParaSinInternet(grabacionId, vozUrl, fondos): Promise<void>`, `estaGuardado(grabacionId): Promise<boolean>`, `borrarGuardado(grabacionId): Promise<void>`.

- [ ] **Paso 1: Escribir la prueba de la parte pura**

Crear `src/lib/sinInternet.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { nombreDeCache, urlsAGuardar } from "./sinInternet.ts";

test("cada grabación tiene su propio cajón", () => {
  assert.equal(nombreDeCache("abc-123"), "mi-voz-abc-123");
  assert.notEqual(nombreDeCache("abc-123"), nombreDeCache("abc-124"));
});

test("guarda la voz y todos los fondos permitidos", () => {
  const urls = urlsAGuardar("https://x/voz.mp3", ["lluvia", "rio"]);
  assert.deepEqual(urls, ["https://x/voz.mp3", "/fondos/lluvia.mp3", "/fondos/rio.mp3"]);
});

test("sin fondos, guarda al menos la voz", () => {
  assert.deepEqual(urlsAGuardar("https://x/voz.mp3", []), ["https://x/voz.mp3"]);
});
```

- [ ] **Paso 2: Correr y ver que falla**

Run: `npm test`
Expected: FAIL — no existe `./sinInternet.ts`.

- [ ] **Paso 3: Escribir el guardado local**

Crear `src/lib/sinInternet.ts`:

```ts
/**
 * Guardado en el celular para escuchar sin internet.
 *
 * Usa la Cache API del navegador, no descargas: el audio queda dentro del
 * espacio de la app, no en la carpeta de descargas del teléfono, así que
 * sigue sin existir un archivo que se pueda mandar a otra persona.
 *
 * Las dos funciones puras (nombre del cajón y lista de URLs) están separadas
 * para poder probarlas sin navegador.
 */
export function nombreDeCache(grabacionId: string): string {
  return `mi-voz-${grabacionId}`;
}

export function urlsAGuardar(vozUrl: string, fondos: string[]): string[] {
  return [vozUrl, ...fondos.map((f) => `/fondos/${f}.mp3`)];
}

export async function guardarParaSinInternet(
  grabacionId: string,
  vozUrl: string,
  fondos: string[],
): Promise<void> {
  const cajon = await caches.open(nombreDeCache(grabacionId));
  await cajon.addAll(urlsAGuardar(vozUrl, fondos));
}

export async function estaGuardado(grabacionId: string): Promise<boolean> {
  const nombres = await caches.keys();
  return nombres.includes(nombreDeCache(grabacionId));
}

export async function borrarGuardado(grabacionId: string): Promise<void> {
  await caches.delete(nombreDeCache(grabacionId));
}
```

- [ ] **Paso 4: Correr y ver que pasa**

Run: `npm test`
Expected: PASS, 46 pruebas en total.

- [ ] **Paso 5: Escribir el service worker**

Crear `public/sw.js`:

```js
/**
 * Sirve desde el cajón lo que ya se guardó.
 *
 * Solo responde con lo cacheado; nunca guarda nada por su cuenta. Guardar es
 * una decisión explícita de la persona (el botón "Guardar en mi celular"),
 * no algo que pase a sus espaldas con su voz.
 */
self.addEventListener("install", () => self.skipWaiting());
self.addEventListener("activate", (e) => e.waitUntil(self.clients.claim()));

self.addEventListener("fetch", (evento) => {
  const url = new URL(evento.request.url);
  const esAudio = url.pathname.endsWith(".mp3") || url.pathname.includes("/object/sign/");
  if (!esAudio) return;

  evento.respondWith(
    caches.match(evento.request, { ignoreSearch: true }).then((guardada) => {
      return guardada || fetch(evento.request);
    }),
  );
});
```

- [ ] **Paso 6: Escribir el manifiesto**

Crear `src/app/manifest.ts`:

```ts
import type { MetadataRoute } from "next";

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "mi-voz",
    short_name: "mi-voz",
    description: "Tus afirmaciones, con tu propia voz.",
    start_url: "/estudio",
    display: "standalone",
    background_color: "#0a0a0a",
    theme_color: "#0a0a0a",
    icons: [{ src: "/icono.png", sizes: "512x512", type: "image/png" }],
  };
}
```

Crear también `public/icono.png` (512×512, fondo oscuro con la marca provisional).

- [ ] **Paso 7: Registrar el service worker y agregar el botón**

En `src/app/(app)/mezclar/[grabacionId]/Mezclador.tsx` agregar dos props nuevas: `planUsuario: Plan` y `fondosPermitidos: string[]`. (`grabacionId` ya se agregó en la Tarea 9.)

⚠️ **La prop se llama `planUsuario`, no `plan`**: dentro de este componente `plan` ya es el plan de mezcla que devuelve `construirPlan`. Usar el mismo nombre para el plan de suscripción lo pisaría.

Importar:

```ts
import { guardarParaSinInternet, estaGuardado } from "@/lib/sinInternet";
import { puedeUsarSinInternet, type Plan } from "@/lib/planes";
```

y agregar este bloque de estado y este botón:

```tsx
  const [guardado, setGuardado] = useState(false);

  useEffect(() => {
    if ("serviceWorker" in navigator) {
      void navigator.serviceWorker.register("/sw.js");
    }
    void estaGuardado(grabacionId).then(setGuardado);
  }, [grabacionId]);

  async function guardarEnElCelular() {
    await guardarParaSinInternet(grabacionId, vozUrl, fondosPermitidos);
    setGuardado(true);
  }
```

```tsx
      {puedeUsarSinInternet(planUsuario) && (
        <button
          onClick={guardarEnElCelular}
          disabled={guardado}
          className="rounded-xl border border-neutral-300 px-4 py-3 disabled:opacity-50"
        >
          {guardado ? "Guardado en tu celular" : "Guardar para escuchar sin internet"}
        </button>
      )}
```

En `page.tsx`, leer el plan del perfil (`select("plan")` sobre `perfiles`) y pasarlo:

```tsx
<Mezclador
  vozUrl={firmada.signedUrl}
  cortesGuardados={grabacion.cortes}
  grabacionId={grabacion.id}
  planUsuario={planDelPerfil}
  fondosPermitidos={fondosPermitidos(planDelPerfil)}
/>
```

- [ ] **Paso 8: Probar sin internet de verdad**

Run: `npm run build && npm run start`

En el celular (o en Chrome con las herramientas de desarrollo): abrir la mezcla con una cuenta de plan `pago`, tocar "Guardar para escuchar sin internet", **poner el teléfono en modo avión**, recargar y tocar "Escuchar". Debe sonar.

⚠️ Las URLs firmadas caducan a la hora. Si al día siguiente falla estando sin internet, es esperado: el service worker sirve la copia cacheada por URL. Verificar que la copia guardada sigue sonando aunque la firma haya vencido; si no, cambiar la ruta de la voz a un endpoint propio estable (`/api/voz/<grabacionId>`) que firme del lado del servidor, y cachear esa URL.

- [ ] **Paso 9: Commit**

```bash
git add src/lib/sinInternet.ts src/lib/sinInternet.test.ts public/sw.js public/icono.png src/app/manifest.ts "src/app/(app)/mezclar"
git commit -m "Escuchar sin internet guardando voz y fondos en el celular"
```

---

## Tarea 13: Portada, despliegue y repaso final

**Files:**
- Create: `src/app/page.tsx` (reemplaza la de `create-next-app`), `src/app/layout.tsx`
- Create: `README.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: la app desplegada.

- [ ] **Paso 1: Escribir la portada**

Reemplazar `src/app/page.tsx`:

```tsx
import Link from "next/link";

export default function Portada() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-lg flex-col justify-center gap-8 px-6">
      <div>
        <h1 className="text-4xl font-semibold leading-tight">
          Escúchate decirte cosas buenas.
        </h1>
        <p className="mt-4 text-lg text-neutral-500">
          Grabas tu voz una vez. Nosotros la dejamos sonando a estudio y la
          ponemos sobre lluvia, río o mar. Después la escuchas cuando la
          necesites.
        </p>
      </div>

      <Link
        href="/ingresar"
        className="rounded-xl bg-neutral-900 px-4 py-4 text-center text-white"
      >
        Grabar mi voz
      </Link>

      <p className="text-sm text-neutral-400">
        Tu voz no se comparte con nadie.{" "}
        <Link href="/privacidad" className="underline">Cómo la cuidamos</Link>
      </p>
    </main>
  );
}
```

- [ ] **Paso 2: Escribir el marco visual**

Reemplazar `src/app/layout.tsx`:

```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "mi-voz — tus afirmaciones con tu propia voz",
  description:
    "Graba tu voz leyendo afirmaciones y escúchalas sobre lluvia, río o mar.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="es">
      <body className="bg-white text-neutral-900 antialiased">{children}</body>
    </html>
  );
}
```

- [ ] **Paso 3: Escribir el README**

Crear `README.md`:

```markdown
# mi-voz

Afirmaciones con la voz de la propia persona, sobre un fondo de lluvia, río o mar.

## Cómo correrlo

    npm install
    cp .env.example .env.local   # llenar con las claves de mandarina-DATA
    npm run dev

## Pruebas

    npm test

Sin librerías de pruebas: runner nativo de Node sobre `src/**/*.test.ts`.

## Cómo está armado

- El audio **se mezcla en vivo en el navegador**. No se genera ningún archivo
  final: se guardan la voz limpia y los fondos, y el plan de mezcla decide qué
  suena y cuándo. Ver `src/lib/audio/plan.ts`.
- El pulido con IA se paga **una vez por grabación** y su resultado (la "voz
  master") sirve para todos los audios que arme la persona después.
- Base de datos: schema `voz` dentro del proyecto Supabase `mandarina-DATA`.
- El login (celular + código por WhatsApp) es el mismo hook de Supabase Auth
  que usa La Mata de los Premios.

## Diseño y plan

- Diseño: `docs/superpowers/specs/2026-07-26-mi-voz-design.md`
- Plan: `docs/superpowers/plans/2026-07-26-mi-voz.md`
```

- [ ] **Paso 4: Correr todo antes de desplegar**

```bash
npm test
npm run build
```
Expected: 46 pruebas en verde y build exitoso.

- [ ] **Paso 5: Desplegar**

```bash
npx vercel --prod
```

En el panel de Vercel, cargar las variables: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `PULIDO_API_KEY`, `LINK_PAGO`.

⚠️ **Cargarlas desde el panel web, no pegándolas desde PowerShell.** Si se cargan desde PowerShell les queda un BOM invisible y Supabase falla solo en producción con `TypeError ... ByteString ... 65279`. El helper `leerEntorno()` lo limpia, pero es mejor no meter el problema.

- [ ] **Paso 6: Prueba completa en producción, desde un celular real**

Con un iPhone y un Android:

1. Abrir la portada, tocar "Grabar mi voz".
2. Entrar con el celular y el código de WhatsApp.
3. Aceptar el consentimiento.
4. Elegir un paquete, editar una frase, grabar de corrido.
5. Esperar el pulido y llegar solo a la mezcla.
6. Cambiar fondo, mover volumen y pausas, escuchar en repetición.
7. Con una cuenta de plan pago: guardar sin internet, modo avión, volver a escuchar.
8. Desde la cuenta: borrar todo y confirmar en Supabase que no quedó ni un archivo.

- [ ] **Paso 7: Commit final**

```bash
git add src/app/page.tsx src/app/layout.tsx README.md
git commit -m "Portada, marco visual y README"
```

---

## Repaso del plan contra el spec

| Requisito del spec | Dónde se cumple |
|---|---|
| Biblioteca de afirmaciones editable | Tarea 1 (paquetes) + Tarea 6 (edición) |
| Grabación de corrido con teleprompter | Tarea 6 |
| Pulido con IA: limpiar, quitar muletillas, cortar frases | Tarea 0 (elección), Tarea 7 (limpieza), Tarea 3 + 8 (frases) |
| Voz master reutilizable, se paga una vez | Tarea 7 (guarda master) + Tarea 9 (guarda cortes) |
| Fondo, volumen del fondo y pausas ajustables | Tarea 8 |
| Ajustes instantáneos, sin renderizar archivos | Tarea 8 (Reproductor) |
| Repetición infinita | Tarea 8 (`enBucle`) |
| El audio nunca sale de la app | Tarea 4 (bucket privado), Tarea 8 (URL firmada), Tarea 12 (Cache API, no descargas) |
| Escuchar sin internet | Tarea 12 |
| Cuenta con celular + código por WhatsApp | Tarea 5 |
| Volver otro día y armar audios nuevos | Tarea 8 (los cortes guardados evitan recalcular) |
| Plan gratis con una grabación, dos fondos | Tarea 10 |
| Pago manual con link de dLocal | Tarea 10 |
| Consentimiento antes de la primera grabación | Tarea 11 |
| Borrar es borrar | Tarea 10 (`borrarTodo`) |
| Toma cruda descartada tras el pulido | Tarea 7 |
| Almacenamiento privado, URLs que caducan | Tarea 4 + Tarea 8 |
| Aviso de privacidad en español | Tarea 11 |
| Schema `voz` en mandarina-DATA | Tarea 4 |

**Fuera de alcance, como manda el spec:** clonación de voz, exportar archivos, compartir, comunidad, notificaciones, app nativa, pasarela automática, estadísticas.
